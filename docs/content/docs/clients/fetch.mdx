---
title: Fetch
description: A fully typed HTTP client for consuming ts-kizuna contracts in the browser or on the server.
---

`@ts-kizuna/fetch` provides `createClient` — a typed wrapper around the native `fetch` API.

<InstallTabs packageName="@ts-kizuna/fetch" />

## `createClient`

```ts
import { createClient } from '@ts-kizuna/fetch';
import { contract } from '@shared/contract';

export const apiClient = createClient(contract, {
    baseUrl: 'http://localhost:3000',
});
```

The returned client mirrors the shape of the contract. Each route becomes a function that accepts the route's typed arguments and returns a Promise resolving to the typed response.

## Calling routes

```ts
// GET /users?page=1&limit=10
const { status, body } = await apiClient.listUsers({
    query: {
        page: 1,
        limit: 10,
    },
});

// body is { users: User[]; total: number }
```

```ts
// POST /users
const result = await apiClient.createUser({
    body: {
        name: 'Alice',
        email: 'alice@example.com',
    },
});

if (result.status === 201) {
    console.log(result.body.id);
} else {
    // result.status === 400
    console.error(result.body.detail);
}
```

```ts
// GET /users/:id
const { status, body } = await apiClient.getUser({
    params: {
        id: 'usr_abc123',
    },
});
```

The `params` field is only required when the route path contains `:param` placeholders. TypeScript enforces this — routes with no path parameters do not accept `params`.

When the route declares a `pathParams` schema, `params` is typed by that schema's output type instead of the path template — the same typing the server-side handler receives. Refinements like `.brand()` or `z.coerce` flow to the caller:

```ts
// contract declares: pathParams: z.object({ id: z.string().brand<'UserId'>() })
const { body } = await apiClient.getUser({
    params: {
        id: userId, // must be a UserId — a plain string is a type error
    },
});
```

## `ClientConfig`

```ts
export interface ClientConfig {
    baseUrl: string;
    baseHeaders?: Record<string, string>;
    credentials?: RequestCredentials;
    fetch?: typeof fetch;
}
```

| Option        | Description                                                  |
| ------------- | ------------------------------------------------------------ |
| `baseUrl`     | Base URL prepended to every route path                       |
| `baseHeaders` | Headers merged into every request                            |
| `credentials` | Passed as `credentials` to every `fetch` call                |
| `fetch`       | Custom fetch implementation (e.g. for testing or a polyfill) |

## Per-request headers

If a route declares a `headers` schema, the client requires those headers:

```ts
// contract declares: headers: z.object({ 'x-request-id': z.string() })
const { body } = await apiClient.getUser({
    params: { id: 'usr_abc123' },
    headers: { 'x-request-id': crypto.randomUUID() },
});
```

Routes without a `headers` schema accept an optional `headers?: Record<string, string>` for arbitrary request headers.

## Per-request fetch options

Pass any `RequestInit` option via `fetchOptions`:

```ts
const { body } = await apiClient.listUsers({
    query: { page: 1 },
    fetchOptions: {
        signal: AbortSignal.timeout(5000),
    },
});
```

## Response type

Each call returns a discriminated union over the route's defined status codes:

```ts
type ListUsersResponse = { status: 200; body: { users: User[]; total: number }; headers: Record<string, string> };
```

Narrow on `status` to access the correctly typed `body`:

```ts
const result = await apiClient.createUser({ body: { name: 'Alice', email: 'alice@example.com' } });

if (result.status === 201) {
    // result.body is User
} else {
    // result.status === 400, result.body is a Problem Details object
}
```

## Validation errors

Routes with a `body` or `query` schema can receive a `400` validation error when the request data fails schema validation. This is automatically included in the response type — you don't need to declare it in your contract.

```ts
const result = await apiClient.createUser({
    body: {
        name: '',
        email: 'not-an-email',
    },
});

if (result.status === 400) {
    for (const issue of result.body.issues) {
        console.log(issue.code, issue.path, issue.message);
        // 'invalid_string_format', ['email'], 'Invalid email'
    }
}
```

Routes without `body` or `query` cannot trigger validation, so the `400` is not included in their response type.

### `isValidationError`

If your contract also declares a `400` response on the same route, the body becomes a union of your type and `ValidationError`. Use the `isValidationError` type guard to distinguish them:

```ts
import { isValidationError } from '@ts-kizuna/fetch';

if (result.status === 400) {
    if (isValidationError(result.body)) {
        // kizuna validation error
    } else {
        // your contract's 400 response
    }
}
```

If your contract does not declare a `400`, you don't need this — `result.status === 400` narrows the body to `ValidationError` directly.

## Reference

- [`createClient`](/docs/reference/create-client)
- [`isValidationError`](/docs/reference/is-validation-error)
