---
title: Mounting
description: Combine a contract and a router into an API object.
---

Once you have a contract and a router, combine them into an API object that an adapter can mount.

## `createApi`

```ts title="src/lib/server.ts"
import { createApi } from '@ts-kizuna/express'; // or @ts-kizuna/fastify, @ts-kizuna/hono, @ts-kizuna/next
import { contract } from './contract';
import { router } from './router';

export const api = createApi({
    contract,
    router,
});
```

## Mounting

Once you have an `api` object, pass it to the adapter's endpoint function:

**Express:**

```ts title="src/index.ts"
import express from 'express';
import { createExpressEndpoints } from '@ts-kizuna/express';
import { api } from './lib/server';

const app = express();
app.use(express.json());
createExpressEndpoints(api, app);
app.listen(3000);
```

**Fastify:**

```ts title="src/index.ts"
import Fastify from 'fastify';
import { fastifyKizuna } from '@ts-kizuna/fastify';
import { api } from './lib/server';

const app = Fastify();
app.register(fastifyKizuna, {
    api,
});
app.listen({
    port: 3000,
});
```

**Hono:**

```ts title="src/index.ts"
import { Hono } from 'hono';
import { createHonoEndpoints } from '@ts-kizuna/hono';
import { api } from './lib/server';

const app = new Hono();
createHonoEndpoints(api, app);

export default app;
```

**Next.js:**

```ts title="src/app/api/[...ts-kizuna]/route.ts"
import { createNextEndpoints } from '@ts-kizuna/next';
import { api } from '../../../lib/server';

export const { GET, HEAD, POST, PUT, PATCH, DELETE, OPTIONS } = createNextEndpoints(api, {
    basePath: '/api',
});
```

## Reference

- [`createApi`](/docs/reference/create-api)

## Next steps

For adapter-specific options (error handling, middleware, `onError`, etc.) see the adapter pages:

- [Express](/docs/adapters/express)
- [Fastify](/docs/adapters/fastify)
- [Hono](/docs/adapters/hono)
- [Next.js](/docs/adapters/next)
