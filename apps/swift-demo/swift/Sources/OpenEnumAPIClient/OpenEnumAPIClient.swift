// Generated by @ts-kizuna/swift. Do not edit by hand.

import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif

public enum OpenEnumAPI {

    /// A user in the system
    public struct User: Codable, Sendable, Equatable {
        public struct Avatar: Codable, Sendable, Equatable {
            public let id: String
            public let url: String

            public init(
                id: String,
                url: String
            ) {
                self.id = id
                self.url = url
            }
        }
        public struct AvatarsItem: Codable, Sendable, Equatable {
            public let id: String
            public let url: String

            public init(
                id: String,
                url: String
            ) {
                self.id = id
                self.url = url
            }
        }
        /// Unique user identifier
        public let id: String
        /// Display name
        public let name: String
        private let _email: String
        /// Email address
        @available(*, deprecated, message: "use `email_address` instead.")
        public var email: String { _email }
        /// Email address
        public let email_address: String?
        /// Family name on the wire as `last_name`, exercises snake_case fidelity through the generators.
        public let last_name: String?
        /// Sibling anonymous objects (`avatar` / `avatars`) exercise inline-object naming where one field name is a prefix of another.
        public let avatar: Avatar?
        public let avatars: [AvatarsItem]?

        private enum CodingKeys: String, CodingKey {
            case id
            case name
            case _email = "email"
            case email_address
            case last_name
            case avatar
            case avatars
        }

        public init(
            id: String,
            name: String,
            email: String,
            email_address: String? = nil,
            last_name: String? = nil,
            avatar: Avatar? = nil,
            avatars: [AvatarsItem]? = nil
        ) {
            self.id = id
            self.name = name
            self._email = email
            self.email_address = email_address
            self.last_name = last_name
            self.avatar = avatar
            self.avatars = avatars
        }
    }

    /// RFC 9457 Problem Details error response.
    public struct ProblemDetails: Codable, Sendable, Equatable {
        public let type: String
        public let title: String
        public let status: Int
        public let detail: String

        public init(
            type: String,
            title: String,
            status: Int,
            detail: String
        ) {
            self.type = type
            self.title = title
            self.status = status
            self.detail = detail
        }
    }

    public enum UserSessionEvent: Codable, Sendable, Equatable {
        public struct Login: Codable, Sendable, Equatable {
            public let kind: String
            public let at: Date
            public let ipAddress: String
            public let userAgent: String

            public init(
                kind: String,
                at: Date,
                ipAddress: String,
                userAgent: String
            ) {
                self.kind = kind
                self.at = at
                self.ipAddress = ipAddress
                self.userAgent = userAgent
            }
        }
        public struct Logout: Codable, Sendable, Equatable {

            public enum Reason: RawRepresentable, Codable, Sendable, Hashable {
                case signed_out
                case session_expired
                case unknown(String)

                public init(rawValue: String) {
                    switch rawValue {
                    case "signed_out": self = .signed_out
                    case "session_expired": self = .session_expired
                    default: self = .unknown(rawValue)
                    }
                }

                public var rawValue: String {
                    switch self {
                    case .signed_out: return "signed_out"
                    case .session_expired: return "session_expired"
                    case let .unknown(value): return value
                    }
                }

                public init(from decoder: Decoder) throws {
                    let container = try decoder.singleValueContainer()
                    self.init(rawValue: try container.decode(String.self))
                }

                public func encode(to encoder: Encoder) throws {
                    var container = encoder.singleValueContainer()
                    try container.encode(rawValue)
                }
            }
            public let kind: String
            public let at: Date
            public let reason: Reason

            public init(
                kind: String,
                at: Date,
                reason: Reason
            ) {
                self.kind = kind
                self.at = at
                self.reason = reason
            }
        }
        case login(Login)
        case logout(Logout)
        public static func login(at: Date, ipAddress: String, userAgent: String) -> UserSessionEvent {
            .login(Login(kind: "login", at: at, ipAddress: ipAddress, userAgent: userAgent))
        }
        public static func logout(at: Date, reason: Logout.Reason) -> UserSessionEvent {
            .logout(Logout(kind: "logout", at: at, reason: reason))
        }

        public var kind: String {
            switch self {
            case .login(let payload): return payload.kind
            case .logout(let payload): return payload.kind
            }
        }

        public var at: Date {
            switch self {
            case .login(let payload): return payload.at
            case .logout(let payload): return payload.at
            }
        }

        private enum DiscriminatorKey: String, CodingKey {
            case discriminator = "kind"
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: DiscriminatorKey.self)
            let kind = try container.decode(String.self, forKey: .discriminator)
            let single = try decoder.singleValueContainer()
            switch kind {
            case "login":
                self = .login(try single.decode(Login.self))
            case "logout":
                self = .logout(try single.decode(Logout.self))
            default:
                throw DecodingError.dataCorruptedError(forKey: .discriminator, in: container, debugDescription: "Unknown discriminator: \(kind)")
            }
        }

        public func encode(to encoder: Encoder) throws {
            var single = encoder.singleValueContainer()
            switch self {
            case .login(let payload):
                try single.encode(payload)
            case .logout(let payload):
                try single.encode(payload)
            }
        }
    }

    public struct CreateUserInput: Codable, Sendable, Equatable {
        /// Display name
        public let name: String
        /// Email address
        public let email: String
        /// Family name. Snake_case wire key, kept verbatim by both clients.
        public let last_name: String?
        /// Phone number in E.164-ish form. Demonstrates a custom-coded validation issue.
        public let phone: String?

        public init(
            name: String,
            email: String,
            last_name: String? = nil,
            phone: String? = nil
        ) {
            self.name = name
            self.email = email
            self.last_name = last_name
            self.phone = phone
        }
    }

    public enum NotificationEvent: Codable, Sendable, Equatable {
        case email(EmailEvent)
        case sms(SmsEvent)
        public static func email(to: String, subject: String) -> NotificationEvent {
            .email(EmailEvent(channel: "email", to: to, subject: subject))
        }
        public static func sms(phone: String, text: String) -> NotificationEvent {
            .sms(SmsEvent(channel: "sms", phone: phone, text: text))
        }

        public var channel: String {
            switch self {
            case .email(let payload): return payload.channel
            case .sms(let payload): return payload.channel
            }
        }

        private enum DiscriminatorKey: String, CodingKey {
            case discriminator = "channel"
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: DiscriminatorKey.self)
            let kind = try container.decode(String.self, forKey: .discriminator)
            let single = try decoder.singleValueContainer()
            switch kind {
            case "email":
                self = .email(try single.decode(EmailEvent.self))
            case "sms":
                self = .sms(try single.decode(SmsEvent.self))
            default:
                throw DecodingError.dataCorruptedError(forKey: .discriminator, in: container, debugDescription: "Unknown discriminator: \(kind)")
            }
        }

        public func encode(to encoder: Encoder) throws {
            var single = encoder.singleValueContainer()
            switch self {
            case .email(let payload):
                try single.encode(payload)
            case .sms(let payload):
                try single.encode(payload)
            }
        }
    }

    public struct EmailEvent: Codable, Sendable, Equatable {
        public let channel: String
        public let to: String
        public let subject: String

        public init(
            channel: String,
            to: String,
            subject: String
        ) {
            self.channel = channel
            self.to = to
            self.subject = subject
        }
    }

    public struct SmsEvent: Codable, Sendable, Equatable {
        public let channel: String
        public let phone: String
        public let text: String

        public init(
            channel: String,
            phone: String,
            text: String
        ) {
            self.channel = channel
            self.phone = phone
            self.text = text
        }
    }

    public enum EventKind: RawRepresentable, Codable, Sendable, Hashable {
        case login
        case logout
        case signup
        case unknown(String)

        public init(rawValue: String) {
            switch rawValue {
            case "login": self = .login
            case "logout": self = .logout
            case "signup": self = .signup
            default: self = .unknown(rawValue)
            }
        }

        public var rawValue: String {
            switch self {
            case .login: return "login"
            case .logout: return "logout"
            case .signup: return "signup"
            case let .unknown(value): return value
            }
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.singleValueContainer()
            self.init(rawValue: try container.decode(String.self))
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.singleValueContainer()
            try container.encode(rawValue)
        }
    }

    public struct EventRecord: Codable, Sendable, Equatable {
        public let id: String
        public let kind: EventKind
        public let occurredAt: Date
        public let userId: String

        public init(
            id: String,
            kind: EventKind,
            occurredAt: Date,
            userId: String
        ) {
            self.id = id
            self.kind = kind
            self.occurredAt = occurredAt
            self.userId = userId
        }
    }
}

public final class OpenEnumAPIClient: Sendable {

    /// Values sent as headers on every request, from the contract's request context.
    public struct RequestContext: Sendable, Equatable {
        public var xPosthogSessionId: String?
        public var xPosthogDistinctId: String?

        public init(xPosthogSessionId: String? = nil, xPosthogDistinctId: String? = nil) {
            self.xPosthogSessionId = xPosthogSessionId
            self.xPosthogDistinctId = xPosthogDistinctId
        }

        var headerFields: [String: String] {
            var fields: [String: String] = [:]
            if let xPosthogSessionId { fields["x-posthog-session-id"] = xPosthogSessionId }
            if let xPosthogDistinctId { fields["x-posthog-distinct-id"] = xPosthogDistinctId }
            return fields
        }
    }

    public struct MultipartFile: Sendable, Equatable {
        public let data: Foundation.Data
        public let filename: String
        public let mimeType: String

        public init(data: Data, filename: String, mimeType: String = "application/octet-stream") {
            self.data = data
            self.filename = filename
            self.mimeType = mimeType
        }
    }

    public struct ValidationError: Codable, Sendable, Equatable {
        public let type: String
        public let title: String
        public let status: Int
        public let detail: String
        public let errors: [ValidationIssue]

        public init(type: String, title: String, status: Int, detail: String, errors: [ValidationIssue]) {
            self.type = type
            self.title = title
            self.status = status
            self.detail = detail
            self.errors = errors
        }
    }

    public struct ValidationIssue: Codable, Sendable, Equatable {
        public let code: String
        public let path: [String]
        public let message: String

        public init(code: String, path: [String], message: String) {
            self.code = code
            self.path = path
            self.message = message
        }
    }

    /// A decoded JSON value of unknown shape. Codable and correctly Equatable by structure.
    public enum JSONValue: Codable, Sendable, Equatable {
        case null
        case bool(Bool)
        case int(Int)
        case double(Double)
        case string(String)
        case array([JSONValue])
        case object([String: JSONValue])

        public init(from decoder: Decoder) throws {
            let container = try decoder.singleValueContainer()
            if container.decodeNil() {
                self = .null
            } else if let value = try? container.decode(Bool.self) {
                self = .bool(value)
            } else if let value = try? container.decode(Int.self) {
                self = .int(value)
            } else if let value = try? container.decode(Double.self) {
                self = .double(value)
            } else if let value = try? container.decode(String.self) {
                self = .string(value)
            } else if let value = try? container.decode([JSONValue].self) {
                self = .array(value)
            } else if let value = try? container.decode([String: JSONValue].self) {
                self = .object(value)
            } else {
                throw DecodingError.dataCorruptedError(in: container, debugDescription: "Unsupported JSON value")
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.singleValueContainer()
            switch self {
            case .null: try container.encodeNil()
            case .bool(let value): try container.encode(value)
            case .int(let value): try container.encode(value)
            case .double(let value): try container.encode(value)
            case .string(let value): try container.encode(value)
            case .array(let value): try container.encode(value)
            case .object(let value): try container.encode(value)
            }
        }
    }
    public let baseURL: URL
    public let session: URLSession
    public let timeout: TimeInterval
    let requestContextHeaders: [String: String]
    public let requestMiddleware: (@Sendable (inout URLRequest) async throws -> Void)?
    public let responseMiddleware: (@Sendable (URLRequest, Foundation.Data, URLResponse) async -> Void)?
    let encoder: JSONEncoder
    let decoder: JSONDecoder

    public init(baseURL: URL, session: URLSession = .shared, timeout: TimeInterval = 30, requestContext: RequestContext = RequestContext(), requestMiddleware: (@Sendable (inout URLRequest) async throws -> Void)? = nil, responseMiddleware: (@Sendable (URLRequest, Foundation.Data, URLResponse) async -> Void)? = nil) {
        self.baseURL = baseURL
        self.session = session
        self.timeout = timeout
        self.requestContextHeaders = requestContext.headerFields
        self.requestMiddleware = requestMiddleware
        self.responseMiddleware = responseMiddleware
        self.encoder = Kizuna.makeJSONEncoder()
        self.decoder = Kizuna.makeJSONDecoder()
    }

    public var users: OpenEnumAPIUsersClient {
        OpenEnumAPIUsersClient(client: self)
    }

    public var health: OpenEnumAPIHealthClient {
        OpenEnumAPIHealthClient(client: self)
    }

    public var notifications: OpenEnumAPINotificationsClient {
        OpenEnumAPINotificationsClient(client: self)
    }

    public var members: OpenEnumAPIMembersClient {
        OpenEnumAPIMembersClient(client: self)
    }

    public var workspace: OpenEnumAPIWorkspaceClient {
        OpenEnumAPIWorkspaceClient(client: self)
    }

    public var invites: OpenEnumAPIInvitesClient {
        OpenEnumAPIInvitesClient(client: self)
    }

    public var assistant: OpenEnumAPIAssistantClient {
        OpenEnumAPIAssistantClient(client: self)
    }

    public enum UsersListUsers {

        public struct Response: Codable, Sendable, Equatable {
            public let users: [OpenEnumAPI.User]
            public let total: Double

            public init(
                users: [OpenEnumAPI.User],
                total: Double
            ) {
                self.users = users
                self.total = total
            }
        }

        public struct Query: Sendable {
            public let page: Int?
            public let limit: Int?

            public init(
                page: Int? = nil,
                limit: Int? = nil
            ) {
                self.page = page
                self.limit = limit
            }

            public static func query(
                page: Int? = nil,
                limit: Int? = nil
            ) -> Self {
                .init(page: page, limit: limit)
            }
        }

        public struct Result: Sendable {
            public let body: Response

            public init(body: Response) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)
            case badRequest(OpenEnumAPIClient.ValidationError)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum UsersExportUsers {

        public struct Result: Sendable {
            public let body: String

            public init(body: String) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum UsersUserBadge {

        public struct Params: Sendable {
            public let id: String

            public init(id: String) {
                self.id = id
            }

            public static func params(id: String) -> Self {
                .init(id: id)
            }
        }

        public struct Result: Sendable {
            public let body: Foundation.Data

            public init(body: Foundation.Data) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)
            case notFound(OpenEnumAPI.ProblemDetails)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum UsersLastSessionEvent {

        public struct Params: Sendable {
            public let id: String

            public init(id: String) {
                self.id = id
            }

            public static func params(id: String) -> Self {
                .init(id: id)
            }
        }

        public struct Result: Sendable {
            public let body: OpenEnumAPI.UserSessionEvent

            public init(body: OpenEnumAPI.UserSessionEvent) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)
            case notFound(OpenEnumAPI.ProblemDetails)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum UsersSearchUsers {

        public struct Response: Codable, Sendable, Equatable {
            public let users: [OpenEnumAPI.User]
            public let nextCursor: Double?

            public init(
                users: [OpenEnumAPI.User],
                nextCursor: Double? = nil
            ) {
                self.users = users
                self.nextCursor = nextCursor
            }
        }

        public struct Query: Sendable {
            public let q: String
            public let limit: Int
            public let cursor: Int

            public init(
                q: String,
                limit: Int,
                cursor: Int
            ) {
                self.q = q
                self.limit = limit
                self.cursor = cursor
            }

            public static func query(
                q: String,
                limit: Int,
                cursor: Int
            ) -> Self {
                .init(q: q, limit: limit, cursor: cursor)
            }
        }

        public struct Result: Sendable {
            public let body: Response

            public init(body: Response) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)
            case badRequest(OpenEnumAPIClient.ValidationError)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum UsersGetUser {

        public struct Params: Sendable {
            public let id: String

            public init(id: String) {
                self.id = id
            }

            public static func params(id: String) -> Self {
                .init(id: id)
            }
        }

        public struct Headers: Sendable {
            public let xRequestId: String

            public init(xRequestId: String) {
                self.xRequestId = xRequestId
            }

            public static func headers(xRequestId: String) -> Self {
                .init(xRequestId: xRequestId)
            }
        }

        public struct Result: Sendable {
            public let body: OpenEnumAPI.User
            public let headers: Headers

            public struct Headers: Sendable {
                public let xRequestId: String?
            }

            public init(body: OpenEnumAPI.User, headers: Headers) {
                self.body = body
                self.headers = headers
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)
            case notFound(OpenEnumAPI.ProblemDetails)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum UsersUserActivity {

        public struct Response: Codable, Sendable, Equatable {
            public let userId: String
            public let year: Int
            public let events: Int

            public init(
                userId: String,
                year: Int,
                events: Int
            ) {
                self.userId = userId
                self.year = year
                self.events = events
            }
        }

        public struct Params: Sendable {
            public let id: String
            public let year: Int

            public init(
                id: String,
                year: Int
            ) {
                self.id = id
                self.year = year
            }

            public static func params(
                id: String,
                year: Int
            ) -> Self {
                .init(id: id, year: year)
            }
        }

        public struct Result: Sendable {
            public let body: Response

            public init(body: Response) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)
            case notFound(OpenEnumAPI.ProblemDetails)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum UsersUserProfile {

        public struct Params: Sendable {
            public let id: String

            public init(id: String) {
                self.id = id
            }

            public static func params(id: String) -> Self {
                .init(id: id)
            }
        }

        public struct Result: Sendable {
            public let body: OpenEnumAPI.User

            public init(body: OpenEnumAPI.User) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)
            case notFound(OpenEnumAPI.ProblemDetails)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum UsersCreateUser {

        public struct Body: Sendable {
            public let payload: OpenEnumAPI.CreateUserInput

            public init(payload: OpenEnumAPI.CreateUserInput) {
                self.payload = payload
            }

            public static func body(
                name: String,
                email: String,
                last_name: String? = nil,
                phone: String? = nil
            ) -> Self {
                .init(payload: OpenEnumAPI.CreateUserInput(name: name, email: email, last_name: last_name, phone: phone))
            }
        }

        public struct Result: Sendable {
            public let body: OpenEnumAPI.User

            public init(body: OpenEnumAPI.User) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)
            case badRequest(OpenEnumAPI.ProblemDetails)
            case validationError(OpenEnumAPIClient.ValidationError)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum UsersDeleteUser {

        public struct Response: Codable, Sendable, Equatable {
            public let success: Bool

            public init(success: Bool) {
                self.success = success
            }
        }

        public struct Params: Sendable {
            public let id: String

            public init(id: String) {
                self.id = id
            }

            public static func params(id: String) -> Self {
                .init(id: id)
            }
        }

        public struct Result: Sendable {
            public let body: Response

            public init(body: Response) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)
            case notFound(OpenEnumAPI.ProblemDetails)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum UsersArchiveUser {

        public struct Response: Codable, Sendable, Equatable {
            public let alreadyArchived: Bool
            public let userId: String

            public init(
                alreadyArchived: Bool,
                userId: String
            ) {
                self.alreadyArchived = alreadyArchived
                self.userId = userId
            }
        }

        public struct Response201: Codable, Sendable, Equatable {
            public let archivedAt: Date
            public let userId: String

            public init(
                archivedAt: Date,
                userId: String
            ) {
                self.archivedAt = archivedAt
                self.userId = userId
            }
        }

        public struct Params: Sendable {
            public let id: String

            public init(id: String) {
                self.id = id
            }

            public static func params(id: String) -> Self {
                .init(id: id)
            }
        }

        public enum Success: Sendable, Equatable {
            case status200(Response)
            case status201(Response201)
        }

        public struct Result: Sendable {
            public let body: Success

            public init(body: Success) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum UsersUploadAvatar {

        public struct Input: Sendable, Equatable {
            public let file: OpenEnumAPIClient.MultipartFile
            public let userId: String

            public init(
                file: OpenEnumAPIClient.MultipartFile,
                userId: String
            ) {
                self.file = file
                self.userId = userId
            }
        }

        public struct Response: Codable, Sendable, Equatable {
            public let size: Double
            public let userId: String

            public init(
                size: Double,
                userId: String
            ) {
                self.size = size
                self.userId = userId
            }
        }

        public struct Body: Sendable {
            public let file: OpenEnumAPIClient.MultipartFile
            public let userId: String

            public init(
                file: OpenEnumAPIClient.MultipartFile,
                userId: String
            ) {
                self.file = file
                self.userId = userId
            }

            public static func body(
                file: OpenEnumAPIClient.MultipartFile,
                userId: String
            ) -> Self {
                .init(file: file, userId: userId)
            }
        }

        public struct Result: Sendable {
            public let body: Response

            public init(body: Response) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)
            case badRequest(OpenEnumAPIClient.ValidationError)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum UsersPingUser {

        public struct Params: Sendable {
            public let id: String

            public init(id: String) {
                self.id = id
            }

            public static func params(id: String) -> Self {
                .init(id: id)
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)
            case badRequest(OpenEnumAPIClient.ValidationError)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum UsersGetMyWork {

        public struct Response: Codable, Sendable, Equatable {
            public let items: [String]
            public let contentType: ResponseContentType

            public init(
                items: [String],
                contentType: ResponseContentType
            ) {
                self.items = items
                self.contentType = contentType
            }
        }

        public enum ResponseContentType: RawRepresentable, Codable, Sendable, Hashable {
            case imageJpeg
            case textPlain
            case videoMp4
            case _3dModel
            case unknown(String)

            public init(rawValue: String) {
                switch rawValue {
                case "image/jpeg": self = .imageJpeg
                case "text-plain": self = .textPlain
                case "video.mp4": self = .videoMp4
                case "3d-model": self = ._3dModel
                default: self = .unknown(rawValue)
                }
            }

            public var rawValue: String {
                switch self {
                case .imageJpeg: return "image/jpeg"
                case .textPlain: return "text-plain"
                case .videoMp4: return "video.mp4"
                case ._3dModel: return "3d-model"
                case let .unknown(value): return value
                }
            }

            public init(from decoder: Decoder) throws {
                let container = try decoder.singleValueContainer()
                self.init(rawValue: try container.decode(String.self))
            }

            public func encode(to encoder: Encoder) throws {
                var container = encoder.singleValueContainer()
                try container.encode(rawValue)
            }
        }

        public enum Success: Sendable, Equatable {
            case status200(Response)
            case status204
        }

        public struct Result: Sendable {
            public let body: Success

            public init(body: Success) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum UsersCheckUser {

        public struct Response: Codable, Sendable, Equatable {
            public let exists: Bool

            public init(exists: Bool) {
                self.exists = exists
            }
        }

        public struct Params: Sendable {
            public let id: String

            public init(id: String) {
                self.id = id
            }

            public static func params(id: String) -> Self {
                .init(id: id)
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)
            case notFound(OpenEnumAPI.ProblemDetails)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum UsersDescribeUsers {

        public struct Response: Codable, Sendable, Equatable {
            public let allow: String

            public init(allow: String) {
                self.allow = allow
            }
        }

        public struct Result: Sendable {
            public let body: Response

            public init(body: Response) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum HealthCheck {

        public struct Response: Codable, Sendable, Equatable {
            public let ok: Bool

            public init(ok: Bool) {
                self.ok = ok
            }
        }

        public struct Result: Sendable {
            public let body: Response

            public init(body: Response) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum HealthVersion {

        public struct Response: Codable, Sendable, Equatable {
            public let version: String

            public init(version: String) {
                self.version = version
            }
        }

        public struct Result: Sendable {
            public let body: Response

            public init(body: Response) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum HealthHistory {

        public struct ResponseItem: Codable, Sendable, Equatable {
            public let ok: Bool
            public let checkedAt: Date

            public init(
                ok: Bool,
                checkedAt: Date
            ) {
                self.ok = ok
                self.checkedAt = checkedAt
            }
        }

        public struct Result: Sendable {
            public let body: [ResponseItem]

            public init(body: [ResponseItem]) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum NotificationsSendNotification {

        public struct Response202: Codable, Sendable, Equatable {
            public let accepted: Bool

            public init(accepted: Bool) {
                self.accepted = accepted
            }
        }

        public struct Body: Sendable {
            public let payload: OpenEnumAPI.NotificationEvent

            public init(payload: OpenEnumAPI.NotificationEvent) {
                self.payload = payload
            }

            public static func body(_ value: OpenEnumAPI.NotificationEvent) -> Self {
                .init(payload: value)
            }
        }

        public struct Result: Sendable {
            public let body: Response202

            public init(body: Response202) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)
            case badRequest(OpenEnumAPIClient.ValidationError)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum NotificationsListEvents {

        public struct Response: Codable, Sendable, Equatable {
            public let events: [OpenEnumAPI.EventRecord]
            public let echo: ResponseEcho

            public init(
                events: [OpenEnumAPI.EventRecord],
                echo: ResponseEcho
            ) {
                self.events = events
                self.echo = echo
            }
        }

        public struct ResponseEcho: Codable, Sendable, Equatable {
            public let since: Date?
            public let kind: OpenEnumAPI.EventKind?
            public let ids: [String]?
            public let label: String?
            public let tagIds: [String]?
            public let sessionId: String?

            public init(
                since: Date? = nil,
                kind: OpenEnumAPI.EventKind? = nil,
                ids: [String]? = nil,
                label: String? = nil,
                tagIds: [String]? = nil,
                sessionId: String? = nil
            ) {
                self.since = since
                self.kind = kind
                self.ids = ids
                self.label = label
                self.tagIds = tagIds
                self.sessionId = sessionId
            }
        }

        public struct Query: Sendable {
            public let since: Date?
            public let kind: OpenEnumAPI.EventKind?
            public let ids: [String]?
            public let label: String?
            public let tagIds: [String]?

            public init(
                since: Date? = nil,
                kind: OpenEnumAPI.EventKind? = nil,
                ids: [String]? = nil,
                label: String? = nil,
                tagIds: [String]? = nil
            ) {
                self.since = since
                self.kind = kind
                self.ids = ids
                self.label = label
                self.tagIds = tagIds
            }

            public static func query(
                since: Date? = nil,
                kind: OpenEnumAPI.EventKind? = nil,
                ids: [String]? = nil,
                label: String? = nil,
                tagIds: [String]? = nil
            ) -> Self {
                .init(since: since, kind: kind, ids: ids, label: label, tagIds: tagIds)
            }
        }

        public struct Result: Sendable {
            public let body: Response

            public init(body: Response) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)
            case badRequest(OpenEnumAPIClient.ValidationError)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum NotificationsValidateConfig {

        public struct Input: Codable, Sendable, Equatable {
            public let `default`: String
            public let interval: Int

            private enum CodingKeys: String, CodingKey {
                case `default`
                case interval
            }

            public init(
                `default`: String,
                interval: Int
            ) {
                self.`default` = `default`
                self.interval = interval
            }
        }

        /// Validation result
        public struct Response: Codable, Sendable, Equatable {
            public let status: String

            public init(status: String) {
                self.status = status
            }
        }

        public struct Body: Sendable {
            public let payload: Input

            public init(payload: Input) {
                self.payload = payload
            }

            public static func body(
                `default`: String,
                interval: Int
            ) -> Self {
                .init(payload: Input(default: `default`, interval: interval))
            }
        }

        public struct Result: Sendable {
            public let body: Response

            public init(body: Response) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)
            case badRequest(OpenEnumAPI.ProblemDetails)
            case unauthorized
            case validationError(OpenEnumAPIClient.ValidationError)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum NotificationsWebhook {

        public struct Response: Codable, Sendable, Equatable {
            public let received: Bool

            public init(received: Bool) {
                self.received = received
            }
        }

        public struct Body: Sendable {
            public let payload: OpenEnumAPIClient.JSONValue

            public init(payload: OpenEnumAPIClient.JSONValue) {
                self.payload = payload
            }

            public static func body(_ value: OpenEnumAPIClient.JSONValue) -> Self {
                .init(payload: value)
            }
        }

        public struct Result: Sendable {
            public let body: Response

            public init(body: Response) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)
            case badRequest(OpenEnumAPIClient.ValidationError)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum MembersListMembers {

        public struct Response: Codable, Sendable, Equatable {
            public let members: [OpenEnumAPI.User]

            public init(members: [OpenEnumAPI.User]) {
                self.members = members
            }
        }

        public struct Result: Sendable {
            public let body: Response

            public init(body: Response) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum MembersInviteMember {

        public struct Input: Codable, Sendable, Equatable {
            public let email: String

            public init(email: String) {
                self.email = email
            }
        }

        public struct Body: Sendable {
            public let payload: Input

            public init(payload: Input) {
                self.payload = payload
            }

            public static func body(email: String) -> Self {
                .init(payload: Input(email: email))
            }
        }

        public struct Result: Sendable {
            public let body: OpenEnumAPI.User

            public init(body: OpenEnumAPI.User) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)
            case conflict(OpenEnumAPI.ProblemDetails)
            case badRequest(OpenEnumAPIClient.ValidationError)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum WorkspaceGetWorkspace {

        public struct Response: Codable, Sendable, Equatable {
            public let id: String
            public let name: String

            public init(
                id: String,
                name: String
            ) {
                self.id = id
                self.name = name
            }
        }

        public struct Result: Sendable {
            public let body: Response

            public init(body: Response) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum WorkspaceDeleteWorkspace {

        public struct Response: Codable, Sendable, Equatable {
            public let ok: Bool

            public init(ok: Bool) {
                self.ok = ok
            }
        }

        public struct Result: Sendable {
            public let body: Response

            public init(body: Response) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum WorkspaceTransfer {

        public struct Input: Codable, Sendable, Equatable {
            public let toUserId: String

            public init(toUserId: String) {
                self.toUserId = toUserId
            }
        }

        public struct Response: Codable, Sendable, Equatable {
            public let ok: Bool

            public init(ok: Bool) {
                self.ok = ok
            }
        }

        public struct Body: Sendable {
            public let payload: Input

            public init(payload: Input) {
                self.payload = payload
            }

            public static func body(toUserId: String) -> Self {
                .init(payload: Input(toUserId: toUserId))
            }
        }

        public struct Result: Sendable {
            public let body: Response

            public init(body: Response) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)
            case badRequest(OpenEnumAPIClient.ValidationError)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum InvitesGetInvite {

        public struct Response: Codable, Sendable, Equatable {
            public let inviteId: String
            public let email: String

            public init(
                inviteId: String,
                email: String
            ) {
                self.inviteId = inviteId
                self.email = email
            }
        }

        public struct Params: Sendable {
            public let token: String

            public init(token: String) {
                self.token = token
            }

            public static func params(token: String) -> Self {
                .init(token: token)
            }
        }

        public struct Result: Sendable {
            public let body: Response

            public init(body: Response) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)
            case notFound(OpenEnumAPI.ProblemDetails)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum InvitesAcceptInvite {

        public struct Input: Codable, Sendable, Equatable {
            public let name: String

            public init(name: String) {
                self.name = name
            }
        }

        public struct Response201: Codable, Sendable, Equatable {
            public let userId: String

            public init(userId: String) {
                self.userId = userId
            }
        }

        public struct Params: Sendable {
            public let token: String

            public init(token: String) {
                self.token = token
            }

            public static func params(token: String) -> Self {
                .init(token: token)
            }
        }

        public struct Body: Sendable {
            public let payload: Input

            public init(payload: Input) {
                self.payload = payload
            }

            public static func body(name: String) -> Self {
                .init(payload: Input(name: name))
            }
        }

        public struct Result: Sendable {
            public let body: Response201

            public init(body: Response201) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)
            case notFound(OpenEnumAPI.ProblemDetails)
            case badRequest(OpenEnumAPIClient.ValidationError)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }

    public enum AssistantReply {

        public struct Input: Codable, Sendable, Equatable {
            public let prompt: String

            public init(prompt: String) {
                self.prompt = prompt
            }
        }

        public struct Delta: Codable, Sendable, Equatable {
            public let text: String

            public init(text: String) {
                self.text = text
            }
        }

        public struct Done: Codable, Sendable, Equatable {
            public let inputTokens: Int
            public let outputTokens: Int

            public init(
                inputTokens: Int,
                outputTokens: Int
            ) {
                self.inputTokens = inputTokens
                self.outputTokens = outputTokens
            }
        }

        public enum ToolCall: Codable, Sendable, Equatable {
            case users_find(ToolCallUsersFind)
            case users_list(ToolCallUsersList)
            case users_create(ToolCallUsersCreate)
            case workspace_read(ToolCallWorkspaceRead)
            case weather_getForecast(ToolCallWeatherGetForecast)
            case charts_plotSignups(ToolCallChartsPlotSignups)
            case countWords(ToolCallCountWords)
            public static func users_find(id: String, input: OpenEnumAPIClient.AssistantReply.ToolCallUsersFindInput) -> ToolCall {
                .users_find(ToolCallUsersFind(id: id, name: "users.find", input: input))
            }
            public static func users_list(id: String, input: OpenEnumAPIClient.AssistantReply.ToolCallUsersListInput) -> ToolCall {
                .users_list(ToolCallUsersList(id: id, name: "users.list", input: input))
            }
            public static func users_create(id: String, input: OpenEnumAPIClient.AssistantReply.ToolCallUsersCreateInput) -> ToolCall {
                .users_create(ToolCallUsersCreate(id: id, name: "users.create", input: input))
            }
            public static func workspace_read(id: String) -> ToolCall {
                .workspace_read(ToolCallWorkspaceRead(id: id, name: "workspace.read"))
            }
            public static func weather_getForecast(id: String, input: OpenEnumAPIClient.AssistantReply.ToolCallWeatherGetForecastInput) -> ToolCall {
                .weather_getForecast(ToolCallWeatherGetForecast(id: id, name: "weather.getForecast", input: input))
            }
            public static func charts_plotSignups(id: String, input: OpenEnumAPIClient.AssistantReply.ToolCallChartsPlotSignupsInput) -> ToolCall {
                .charts_plotSignups(ToolCallChartsPlotSignups(id: id, name: "charts.plotSignups", input: input))
            }
            public static func countWords(id: String, input: OpenEnumAPIClient.AssistantReply.ToolCallCountWordsInput) -> ToolCall {
                .countWords(ToolCallCountWords(id: id, name: "countWords", input: input))
            }

            public var id: String {
                switch self {
                case .users_find(let payload): return payload.id
                case .users_list(let payload): return payload.id
                case .users_create(let payload): return payload.id
                case .workspace_read(let payload): return payload.id
                case .weather_getForecast(let payload): return payload.id
                case .charts_plotSignups(let payload): return payload.id
                case .countWords(let payload): return payload.id
                }
            }

            public var name: String {
                switch self {
                case .users_find(let payload): return payload.name
                case .users_list(let payload): return payload.name
                case .users_create(let payload): return payload.name
                case .workspace_read(let payload): return payload.name
                case .weather_getForecast(let payload): return payload.name
                case .charts_plotSignups(let payload): return payload.name
                case .countWords(let payload): return payload.name
                }
            }

            private enum DiscriminatorKey: String, CodingKey {
                case discriminator = "name"
            }

            public init(from decoder: Decoder) throws {
                let container = try decoder.container(keyedBy: DiscriminatorKey.self)
                let kind = try container.decode(String.self, forKey: .discriminator)
                let single = try decoder.singleValueContainer()
                switch kind {
                case "users.find":
                    self = .users_find(try single.decode(ToolCallUsersFind.self))
                case "users.list":
                    self = .users_list(try single.decode(ToolCallUsersList.self))
                case "users.create":
                    self = .users_create(try single.decode(ToolCallUsersCreate.self))
                case "workspace.read":
                    self = .workspace_read(try single.decode(ToolCallWorkspaceRead.self))
                case "weather.getForecast":
                    self = .weather_getForecast(try single.decode(ToolCallWeatherGetForecast.self))
                case "charts.plotSignups":
                    self = .charts_plotSignups(try single.decode(ToolCallChartsPlotSignups.self))
                case "countWords":
                    self = .countWords(try single.decode(ToolCallCountWords.self))
                default:
                    throw DecodingError.dataCorruptedError(forKey: .discriminator, in: container, debugDescription: "Unknown discriminator: \(kind)")
                }
            }

            public func encode(to encoder: Encoder) throws {
                var single = encoder.singleValueContainer()
                switch self {
                case .users_find(let payload):
                    try single.encode(payload)
                case .users_list(let payload):
                    try single.encode(payload)
                case .users_create(let payload):
                    try single.encode(payload)
                case .workspace_read(let payload):
                    try single.encode(payload)
                case .weather_getForecast(let payload):
                    try single.encode(payload)
                case .charts_plotSignups(let payload):
                    try single.encode(payload)
                case .countWords(let payload):
                    try single.encode(payload)
                }
            }
        }

        public struct ToolCallUsersFind: Codable, Sendable, Equatable {
            public let id: String
            public let name: String
            public let input: ToolCallUsersFindInput

            public init(
                id: String,
                name: String,
                input: ToolCallUsersFindInput
            ) {
                self.id = id
                self.name = name
                self.input = input
            }
        }

        public struct ToolCallUsersFindInput: Codable, Sendable, Equatable {
            public let params: ToolCallUsersFindInputParams

            public init(params: ToolCallUsersFindInputParams) {
                self.params = params
            }
        }

        public struct ToolCallUsersFindInputParams: Codable, Sendable, Equatable {
            public let id: String

            public init(id: String) {
                self.id = id
            }
        }

        public struct ToolCallUsersList: Codable, Sendable, Equatable {
            public let id: String
            public let name: String
            public let input: ToolCallUsersListInput

            public init(
                id: String,
                name: String,
                input: ToolCallUsersListInput
            ) {
                self.id = id
                self.name = name
                self.input = input
            }
        }

        public struct ToolCallUsersListInput: Codable, Sendable, Equatable {
            public let query: ToolCallUsersListInputQuery?

            public init(query: ToolCallUsersListInputQuery? = nil) {
                self.query = query
            }
        }

        public struct ToolCallUsersListInputQuery: Codable, Sendable, Equatable {
            /// Page number, starting at 1
            public let page: Int?
            /// Page size (1–100)
            public let limit: Int?

            public init(
                page: Int? = nil,
                limit: Int? = nil
            ) {
                self.page = page
                self.limit = limit
            }
        }

        public struct ToolCallUsersCreate: Codable, Sendable, Equatable {
            public let id: String
            public let name: String
            public let input: ToolCallUsersCreateInput

            public init(
                id: String,
                name: String,
                input: ToolCallUsersCreateInput
            ) {
                self.id = id
                self.name = name
                self.input = input
            }
        }

        public struct ToolCallUsersCreateInput: Codable, Sendable, Equatable {
            public let body: OpenEnumAPI.CreateUserInput

            public init(body: OpenEnumAPI.CreateUserInput) {
                self.body = body
            }
        }

        public struct ToolCallWorkspaceRead: Codable, Sendable, Equatable {
            public let id: String
            public let name: String

            public init(
                id: String,
                name: String
            ) {
                self.id = id
                self.name = name
            }
        }

        public struct ToolCallWeatherGetForecast: Codable, Sendable, Equatable {
            public let id: String
            public let name: String
            public let input: ToolCallWeatherGetForecastInput

            public init(
                id: String,
                name: String,
                input: ToolCallWeatherGetForecastInput
            ) {
                self.id = id
                self.name = name
                self.input = input
            }
        }

        public struct ToolCallWeatherGetForecastInput: Codable, Sendable, Equatable {
            public let city: String
            public let unit: ToolCallWeatherGetForecastInputUnit?

            public init(
                city: String,
                unit: ToolCallWeatherGetForecastInputUnit? = nil
            ) {
                self.city = city
                self.unit = unit
            }
        }

        public enum ToolCallWeatherGetForecastInputUnit: RawRepresentable, Codable, Sendable, Hashable {
            case celsius
            case fahrenheit
            case unknown(String)

            public init(rawValue: String) {
                switch rawValue {
                case "celsius": self = .celsius
                case "fahrenheit": self = .fahrenheit
                default: self = .unknown(rawValue)
                }
            }

            public var rawValue: String {
                switch self {
                case .celsius: return "celsius"
                case .fahrenheit: return "fahrenheit"
                case let .unknown(value): return value
                }
            }

            public init(from decoder: Decoder) throws {
                let container = try decoder.singleValueContainer()
                self.init(rawValue: try container.decode(String.self))
            }

            public func encode(to encoder: Encoder) throws {
                var container = encoder.singleValueContainer()
                try container.encode(rawValue)
            }
        }

        public struct ToolCallChartsPlotSignups: Codable, Sendable, Equatable {
            public let id: String
            public let name: String
            public let input: ToolCallChartsPlotSignupsInput

            public init(
                id: String,
                name: String,
                input: ToolCallChartsPlotSignupsInput
            ) {
                self.id = id
                self.name = name
                self.input = input
            }
        }

        public struct ToolCallChartsPlotSignupsInput: Codable, Sendable, Equatable {
            public let days: Int

            public init(days: Int) {
                self.days = days
            }
        }

        public struct ToolCallCountWords: Codable, Sendable, Equatable {
            public let id: String
            public let name: String
            public let input: ToolCallCountWordsInput

            public init(
                id: String,
                name: String,
                input: ToolCallCountWordsInput
            ) {
                self.id = id
                self.name = name
                self.input = input
            }
        }

        public struct ToolCallCountWordsInput: Codable, Sendable, Equatable {
            public let text: String

            public init(text: String) {
                self.text = text
            }
        }

        public enum ToolResult: Codable, Sendable, Equatable {
            case users_find(ToolResultUsersFind)
            case users_list(ToolResultUsersList)
            case users_create(ToolResultUsersCreate)
            case workspace_read(ToolResultWorkspaceRead)
            case weather_getForecast(ToolResultWeatherGetForecast)
            case charts_plotSignups(ToolResultChartsPlotSignups)
            case countWords(ToolResultCountWords)
            public static func users_find(id: String, output: OpenEnumAPIClient.AssistantReply.ToolResultUsersFindOutput) -> ToolResult {
                .users_find(ToolResultUsersFind(id: id, name: "users.find", output: output))
            }
            public static func users_list(id: String, output: OpenEnumAPIClient.AssistantReply.ToolResultUsersListOutput) -> ToolResult {
                .users_list(ToolResultUsersList(id: id, name: "users.list", output: output))
            }
            public static func users_create(id: String, output: OpenEnumAPIClient.AssistantReply.ToolResultUsersCreateOutput) -> ToolResult {
                .users_create(ToolResultUsersCreate(id: id, name: "users.create", output: output))
            }
            public static func workspace_read(id: String, output: OpenEnumAPIClient.AssistantReply.ToolResultWorkspaceReadOutput) -> ToolResult {
                .workspace_read(ToolResultWorkspaceRead(id: id, name: "workspace.read", output: output))
            }
            public static func weather_getForecast(id: String, output: OpenEnumAPIClient.AssistantReply.ToolResultWeatherGetForecastOutput) -> ToolResult {
                .weather_getForecast(ToolResultWeatherGetForecast(id: id, name: "weather.getForecast", output: output))
            }
            public static func charts_plotSignups(id: String, output: OpenEnumAPIClient.AssistantReply.ToolResultChartsPlotSignupsOutput) -> ToolResult {
                .charts_plotSignups(ToolResultChartsPlotSignups(id: id, name: "charts.plotSignups", output: output))
            }
            public static func countWords(id: String, output: OpenEnumAPIClient.AssistantReply.ToolResultCountWordsOutput) -> ToolResult {
                .countWords(ToolResultCountWords(id: id, name: "countWords", output: output))
            }

            public var id: String {
                switch self {
                case .users_find(let payload): return payload.id
                case .users_list(let payload): return payload.id
                case .users_create(let payload): return payload.id
                case .workspace_read(let payload): return payload.id
                case .weather_getForecast(let payload): return payload.id
                case .charts_plotSignups(let payload): return payload.id
                case .countWords(let payload): return payload.id
                }
            }

            public var name: String {
                switch self {
                case .users_find(let payload): return payload.name
                case .users_list(let payload): return payload.name
                case .users_create(let payload): return payload.name
                case .workspace_read(let payload): return payload.name
                case .weather_getForecast(let payload): return payload.name
                case .charts_plotSignups(let payload): return payload.name
                case .countWords(let payload): return payload.name
                }
            }

            private enum DiscriminatorKey: String, CodingKey {
                case discriminator = "name"
            }

            public init(from decoder: Decoder) throws {
                let container = try decoder.container(keyedBy: DiscriminatorKey.self)
                let kind = try container.decode(String.self, forKey: .discriminator)
                let single = try decoder.singleValueContainer()
                switch kind {
                case "users.find":
                    self = .users_find(try single.decode(ToolResultUsersFind.self))
                case "users.list":
                    self = .users_list(try single.decode(ToolResultUsersList.self))
                case "users.create":
                    self = .users_create(try single.decode(ToolResultUsersCreate.self))
                case "workspace.read":
                    self = .workspace_read(try single.decode(ToolResultWorkspaceRead.self))
                case "weather.getForecast":
                    self = .weather_getForecast(try single.decode(ToolResultWeatherGetForecast.self))
                case "charts.plotSignups":
                    self = .charts_plotSignups(try single.decode(ToolResultChartsPlotSignups.self))
                case "countWords":
                    self = .countWords(try single.decode(ToolResultCountWords.self))
                default:
                    throw DecodingError.dataCorruptedError(forKey: .discriminator, in: container, debugDescription: "Unknown discriminator: \(kind)")
                }
            }

            public func encode(to encoder: Encoder) throws {
                var single = encoder.singleValueContainer()
                switch self {
                case .users_find(let payload):
                    try single.encode(payload)
                case .users_list(let payload):
                    try single.encode(payload)
                case .users_create(let payload):
                    try single.encode(payload)
                case .workspace_read(let payload):
                    try single.encode(payload)
                case .weather_getForecast(let payload):
                    try single.encode(payload)
                case .charts_plotSignups(let payload):
                    try single.encode(payload)
                case .countWords(let payload):
                    try single.encode(payload)
                }
            }
        }

        public struct ToolResultUsersFind: Codable, Sendable, Equatable {
            public let id: String
            public let name: String
            public let output: ToolResultUsersFindOutput

            public init(
                id: String,
                name: String,
                output: ToolResultUsersFindOutput
            ) {
                self.id = id
                self.name = name
                self.output = output
            }
        }

        public struct ToolResultUsersFindOutput: Codable, Sendable, Equatable {
            /// The HTTP status the route answered with
            public let status: Int
            /// A user in the system
            public let body: OpenEnumAPI.User

            public init(
                status: Int,
                body: OpenEnumAPI.User
            ) {
                self.status = status
                self.body = body
            }
        }

        public struct ToolResultUsersList: Codable, Sendable, Equatable {
            public let id: String
            public let name: String
            public let output: ToolResultUsersListOutput

            public init(
                id: String,
                name: String,
                output: ToolResultUsersListOutput
            ) {
                self.id = id
                self.name = name
                self.output = output
            }
        }

        public struct ToolResultUsersListOutput: Codable, Sendable, Equatable {
            /// The HTTP status the route answered with
            public let status: Int
            public let body: ToolResultUsersListOutputBody

            public init(
                status: Int,
                body: ToolResultUsersListOutputBody
            ) {
                self.status = status
                self.body = body
            }
        }

        public struct ToolResultUsersListOutputBody: Codable, Sendable, Equatable {
            public let users: [OpenEnumAPI.User]
            public let total: Double

            public init(
                users: [OpenEnumAPI.User],
                total: Double
            ) {
                self.users = users
                self.total = total
            }
        }

        public struct ToolResultUsersCreate: Codable, Sendable, Equatable {
            public let id: String
            public let name: String
            public let output: ToolResultUsersCreateOutput

            public init(
                id: String,
                name: String,
                output: ToolResultUsersCreateOutput
            ) {
                self.id = id
                self.name = name
                self.output = output
            }
        }

        public struct ToolResultUsersCreateOutput: Codable, Sendable, Equatable {
            /// The HTTP status the route answered with
            public let status: Int
            /// A user in the system
            public let body: OpenEnumAPI.User

            public init(
                status: Int,
                body: OpenEnumAPI.User
            ) {
                self.status = status
                self.body = body
            }
        }

        public struct ToolResultWorkspaceRead: Codable, Sendable, Equatable {
            public let id: String
            public let name: String
            public let output: ToolResultWorkspaceReadOutput

            public init(
                id: String,
                name: String,
                output: ToolResultWorkspaceReadOutput
            ) {
                self.id = id
                self.name = name
                self.output = output
            }
        }

        public struct ToolResultWorkspaceReadOutput: Codable, Sendable, Equatable {
            /// The HTTP status the route answered with
            public let status: Int
            public let body: ToolResultWorkspaceReadOutputBody

            public init(
                status: Int,
                body: ToolResultWorkspaceReadOutputBody
            ) {
                self.status = status
                self.body = body
            }
        }

        public struct ToolResultWorkspaceReadOutputBody: Codable, Sendable, Equatable {
            public let id: String
            public let name: String

            public init(
                id: String,
                name: String
            ) {
                self.id = id
                self.name = name
            }
        }

        public struct ToolResultWeatherGetForecast: Codable, Sendable, Equatable {
            public let id: String
            public let name: String
            public let output: ToolResultWeatherGetForecastOutput

            public init(
                id: String,
                name: String,
                output: ToolResultWeatherGetForecastOutput
            ) {
                self.id = id
                self.name = name
                self.output = output
            }
        }

        public struct ToolResultWeatherGetForecastOutput: Codable, Sendable, Equatable {
            public let temperature: Double
            public let unit: ToolResultWeatherGetForecastOutputUnit
            public let summary: String

            public init(
                temperature: Double,
                unit: ToolResultWeatherGetForecastOutputUnit,
                summary: String
            ) {
                self.temperature = temperature
                self.unit = unit
                self.summary = summary
            }
        }

        public enum ToolResultWeatherGetForecastOutputUnit: RawRepresentable, Codable, Sendable, Hashable {
            case celsius
            case fahrenheit
            case unknown(String)

            public init(rawValue: String) {
                switch rawValue {
                case "celsius": self = .celsius
                case "fahrenheit": self = .fahrenheit
                default: self = .unknown(rawValue)
                }
            }

            public var rawValue: String {
                switch self {
                case .celsius: return "celsius"
                case .fahrenheit: return "fahrenheit"
                case let .unknown(value): return value
                }
            }

            public init(from decoder: Decoder) throws {
                let container = try decoder.singleValueContainer()
                self.init(rawValue: try container.decode(String.self))
            }

            public func encode(to encoder: Encoder) throws {
                var container = encoder.singleValueContainer()
                try container.encode(rawValue)
            }
        }

        public struct ToolResultChartsPlotSignups: Codable, Sendable, Equatable {
            public let id: String
            public let name: String
            public let output: ToolResultChartsPlotSignupsOutput

            public init(
                id: String,
                name: String,
                output: ToolResultChartsPlotSignupsOutput
            ) {
                self.id = id
                self.name = name
                self.output = output
            }
        }

        public struct ToolResultChartsPlotSignupsOutput: Codable, Sendable, Equatable {
            public let points: [ToolResultChartsPlotSignupsOutputPointsItem]

            public init(points: [ToolResultChartsPlotSignupsOutputPointsItem]) {
                self.points = points
            }
        }

        public struct ToolResultChartsPlotSignupsOutputPointsItem: Codable, Sendable, Equatable {
            public let date: String
            public let signups: Int

            public init(
                date: String,
                signups: Int
            ) {
                self.date = date
                self.signups = signups
            }
        }

        public struct ToolResultCountWords: Codable, Sendable, Equatable {
            public let id: String
            public let name: String
            public let output: ToolResultCountWordsOutput

            public init(
                id: String,
                name: String,
                output: ToolResultCountWordsOutput
            ) {
                self.id = id
                self.name = name
                self.output = output
            }
        }

        public struct ToolResultCountWordsOutput: Codable, Sendable, Equatable {
            public let words: Int

            public init(words: Int) {
                self.words = words
            }
        }

        public struct ToolError: Codable, Sendable, Equatable {
            public let id: String
            public let name: ToolErrorName
            public let message: String

            public init(
                id: String,
                name: ToolErrorName,
                message: String
            ) {
                self.id = id
                self.name = name
                self.message = message
            }
        }

        public enum ToolErrorName: RawRepresentable, Codable, Sendable, Hashable {
            case usersFind
            case usersList
            case usersCreate
            case workspaceRead
            case weatherGetForecast
            case chartsPlotSignups
            case countWords
            case unknown(String)

            public init(rawValue: String) {
                switch rawValue {
                case "users.find": self = .usersFind
                case "users.list": self = .usersList
                case "users.create": self = .usersCreate
                case "workspace.read": self = .workspaceRead
                case "weather.getForecast": self = .weatherGetForecast
                case "charts.plotSignups": self = .chartsPlotSignups
                case "countWords": self = .countWords
                default: self = .unknown(rawValue)
                }
            }

            public var rawValue: String {
                switch self {
                case .usersFind: return "users.find"
                case .usersList: return "users.list"
                case .usersCreate: return "users.create"
                case .workspaceRead: return "workspace.read"
                case .weatherGetForecast: return "weather.getForecast"
                case .chartsPlotSignups: return "charts.plotSignups"
                case .countWords: return "countWords"
                case let .unknown(value): return value
                }
            }

            public init(from decoder: Decoder) throws {
                let container = try decoder.singleValueContainer()
                self.init(rawValue: try container.decode(String.self))
            }

            public func encode(to encoder: Encoder) throws {
                var container = encoder.singleValueContainer()
                try container.encode(rawValue)
            }
        }

        public struct Body: Sendable {
            public let payload: Input

            public init(payload: Input) {
                self.payload = payload
            }

            public static func body(prompt: String) -> Self {
                .init(payload: Input(prompt: prompt))
            }
        }

        public enum Event: Sendable, Equatable {
            case delta(Delta)
            case done(Done)
            case tool_call(ToolCall)
            case tool_result(ToolResult)
            case tool_error(ToolError)
        }

        public struct ToolCallRecord: Identifiable, Sendable, Equatable {
            /// How far along the call is.
            public enum State: Sendable, Equatable {
                case running
                case done
                case failed
            }

            public let id: String
            public let name: String
            public var state: State
            /// The call as it arrived, to read its input.
            public var call: ToolCall?
            /// The result once it answered, to read its output.
            public var result: ToolResult?
            /// What the tool reported when it failed.
            public var message: String?
        }

        /// Fold a stream's events into one row per tool call, in the order the calls arrived.
        public static func readToolCalls(_ events: [Event]) -> [ToolCallRecord] {
            var order: [String] = []
            var calls: [String: ToolCallRecord] = [:]

            func at(_ id: String, _ name: String) -> ToolCallRecord {
                if let existing = calls[id] { return existing }
                order.append(id)
                return ToolCallRecord(id: id, name: name, state: .running, call: nil, result: nil, message: nil)
            }

            for event in events {
                switch event {
                case .tool_call(let payload):
                    var tracked = at(payload.id, payload.name)
                    tracked.call = payload
                    calls[payload.id] = tracked
                case .tool_result(let payload):
                    var tracked = at(payload.id, payload.name)
                    tracked.state = .done
                    tracked.result = payload
                    calls[payload.id] = tracked
                case .tool_error(let payload):
                    var tracked = at(payload.id, payload.name.rawValue)
                    tracked.state = .failed
                    tracked.message = payload.message
                    calls[payload.id] = tracked
                default:
                    continue
                }
            }

            return order.compactMap { calls[$0] }
        }

        public struct Result: Sendable {
            public let body: AsyncThrowingStream<Event, Swift.Error>

            public init(body: AsyncThrowingStream<Event, Swift.Error>) {
                self.body = body
            }
        }

        public enum Failure: Swift.Error, Sendable, KizunaDecodableFailure {
            case requestFailed(Swift.Error)
            case invalidRequest
            case cancelled
            case invalidResponse
            case decoding(Swift.Error, statusCode: Int, data: Foundation.Data)
            case unexpectedStatus(Int, Foundation.Data)
            case badRequest(OpenEnumAPI.ProblemDetails)
            case validationError(OpenEnumAPIClient.ValidationError)

            public var isCancelled: Bool {
                if case .cancelled = self { return true }
                return false
            }
        }
    }
}

public struct OpenEnumAPIUsersClient: Sendable {
    private let client: OpenEnumAPIClient

    init(client: OpenEnumAPIClient) {
        self.client = client
    }

    /// List users with pagination
    public func listUsers(_ query: OpenEnumAPIClient.UsersListUsers.Query = .query()) async throws(OpenEnumAPIClient.UsersListUsers.Failure) -> OpenEnumAPIClient.UsersListUsers.Result {
        let path = "/users"
        var queryItems: [URLQueryItem] = []
        queryItems += Kizuna.queryItems(name: "page", value: query.page)
        queryItems += Kizuna.queryItems(name: "limit", value: query.limit)
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: queryItems, failure: OpenEnumAPIClient.UsersListUsers.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "GET"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.UsersListUsers.Failure.self)
        switch statusCode {
        case 200:
            let body = try Kizuna.decode(OpenEnumAPIClient.UsersListUsers.Response.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersListUsers.Failure.self)
            return OpenEnumAPIClient.UsersListUsers.Result(body: body)
        case 400:
            let payload = try Kizuna.decode(OpenEnumAPIClient.ValidationError.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersListUsers.Failure.self)
            throw OpenEnumAPIClient.UsersListUsers.Failure.badRequest(payload)
        default:
            throw OpenEnumAPIClient.UsersListUsers.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// Export users as CSV, exercises a non-JSON (text/csv) raw response body
    public func exportUsers() async throws(OpenEnumAPIClient.UsersExportUsers.Failure) -> OpenEnumAPIClient.UsersExportUsers.Result {
        let path = "/users/export"
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.UsersExportUsers.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "GET"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.UsersExportUsers.Failure.self)
        switch statusCode {
        case 200:
            let body = String(decoding: data, as: UTF8.self)
            return OpenEnumAPIClient.UsersExportUsers.Result(body: body)
        default:
            throw OpenEnumAPIClient.UsersExportUsers.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// Download a user badge, exercises a binary (BinarySchema) response body under a cache policy and an ETag
    public func userBadge(_ params: OpenEnumAPIClient.UsersUserBadge.Params) async throws(OpenEnumAPIClient.UsersUserBadge.Failure) -> OpenEnumAPIClient.UsersUserBadge.Result {
        var path = "/users/:id/badge"
        path = path.replacingOccurrences(of: ":id", with: Kizuna.encodePathSegment(params.id))
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.UsersUserBadge.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "GET"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.UsersUserBadge.Failure.self)
        switch statusCode {
        case 200:
            let body = data
            return OpenEnumAPIClient.UsersUserBadge.Result(body: body)
        case 404:
            let payload = try Kizuna.decode(OpenEnumAPI.ProblemDetails.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersUserBadge.Failure.self)
            throw OpenEnumAPIClient.UsersUserBadge.Failure.notFound(payload)
        default:
            throw OpenEnumAPIClient.UsersUserBadge.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// A user's most recent login or logout, inline union variants nest under the User model in native clients
    public func lastSessionEvent(_ params: OpenEnumAPIClient.UsersLastSessionEvent.Params) async throws(OpenEnumAPIClient.UsersLastSessionEvent.Failure) -> OpenEnumAPIClient.UsersLastSessionEvent.Result {
        var path = "/users/:id/last-session-event"
        path = path.replacingOccurrences(of: ":id", with: Kizuna.encodePathSegment(params.id))
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.UsersLastSessionEvent.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "GET"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.UsersLastSessionEvent.Failure.self)
        switch statusCode {
        case 200:
            let body = try Kizuna.decode(OpenEnumAPI.UserSessionEvent.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersLastSessionEvent.Failure.self)
            return OpenEnumAPIClient.UsersLastSessionEvent.Result(body: body)
        case 404:
            let payload = try Kizuna.decode(OpenEnumAPI.ProblemDetails.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersLastSessionEvent.Failure.self)
            throw OpenEnumAPIClient.UsersLastSessionEvent.Failure.notFound(payload)
        default:
            throw OpenEnumAPIClient.UsersLastSessionEvent.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// Search users, required coerced limit and cursor
    public func searchUsers(_ query: OpenEnumAPIClient.UsersSearchUsers.Query) async throws(OpenEnumAPIClient.UsersSearchUsers.Failure) -> OpenEnumAPIClient.UsersSearchUsers.Result {
        let path = "/users/search"
        var queryItems: [URLQueryItem] = []
        queryItems += Kizuna.queryItems(name: "q", value: query.q)
        queryItems += Kizuna.queryItems(name: "limit", value: query.limit)
        queryItems += Kizuna.queryItems(name: "cursor", value: query.cursor)
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: queryItems, failure: OpenEnumAPIClient.UsersSearchUsers.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "GET"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.UsersSearchUsers.Failure.self)
        switch statusCode {
        case 200:
            let body = try Kizuna.decode(OpenEnumAPIClient.UsersSearchUsers.Response.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersSearchUsers.Failure.self)
            return OpenEnumAPIClient.UsersSearchUsers.Result(body: body)
        case 400:
            let payload = try Kizuna.decode(OpenEnumAPIClient.ValidationError.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersSearchUsers.Failure.self)
            throw OpenEnumAPIClient.UsersSearchUsers.Failure.badRequest(payload)
        default:
            throw OpenEnumAPIClient.UsersSearchUsers.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// Get a user by id
    public func getUser(_ params: OpenEnumAPIClient.UsersGetUser.Params, _ headers: OpenEnumAPIClient.UsersGetUser.Headers) async throws(OpenEnumAPIClient.UsersGetUser.Failure) -> OpenEnumAPIClient.UsersGetUser.Result {
        var path = "/users/:id"
        path = path.replacingOccurrences(of: ":id", with: Kizuna.encodePathSegment(params.id))
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.UsersGetUser.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "GET"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        Kizuna.setHeader(&request, name: "x-request-id", value: headers.xRequestId)
        let (data, statusCode, httpResponse) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.UsersGetUser.Failure.self)
        switch statusCode {
        case 200:
            let body = try Kizuna.decode(OpenEnumAPI.User.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersGetUser.Failure.self)
            let xRequestId = httpResponse.value(forHTTPHeaderField: "x-request-id")
            return OpenEnumAPIClient.UsersGetUser.Result(body: body, headers: .init(xRequestId: xRequestId))
        case 404:
            let payload = try Kizuna.decode(OpenEnumAPI.ProblemDetails.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersGetUser.Failure.self)
            throw OpenEnumAPIClient.UsersGetUser.Failure.notFound(payload)
        default:
            throw OpenEnumAPIClient.UsersGetUser.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// Get a year of user activity, exercising two typed path params (a string id and a coerced int year) and a cache policy on both a success and an error response
    public func userActivity(_ params: OpenEnumAPIClient.UsersUserActivity.Params) async throws(OpenEnumAPIClient.UsersUserActivity.Failure) -> OpenEnumAPIClient.UsersUserActivity.Result {
        var path = "/users/:id/activity/:year"
        path = path.replacingOccurrences(of: ":id", with: Kizuna.encodePathSegment(params.id))
        path = path.replacingOccurrences(of: ":year", with: Kizuna.encodePathSegment(params.year))
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.UsersUserActivity.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "GET"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.UsersUserActivity.Failure.self)
        switch statusCode {
        case 200:
            let body = try Kizuna.decode(OpenEnumAPIClient.UsersUserActivity.Response.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersUserActivity.Failure.self)
            return OpenEnumAPIClient.UsersUserActivity.Result(body: body)
        case 404:
            let payload = try Kizuna.decode(OpenEnumAPI.ProblemDetails.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersUserActivity.Failure.self)
            throw OpenEnumAPIClient.UsersUserActivity.Failure.notFound(payload)
        default:
            throw OpenEnumAPIClient.UsersUserActivity.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// Get a user profile, exercises an ETag and the 304 a matching If-None-Match answers with
    public func userProfile(_ params: OpenEnumAPIClient.UsersUserProfile.Params) async throws(OpenEnumAPIClient.UsersUserProfile.Failure) -> OpenEnumAPIClient.UsersUserProfile.Result {
        var path = "/users/:id/profile"
        path = path.replacingOccurrences(of: ":id", with: Kizuna.encodePathSegment(params.id))
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.UsersUserProfile.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "GET"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.UsersUserProfile.Failure.self)
        switch statusCode {
        case 200:
            let body = try Kizuna.decode(OpenEnumAPI.User.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersUserProfile.Failure.self)
            return OpenEnumAPIClient.UsersUserProfile.Result(body: body)
        case 404:
            let payload = try Kizuna.decode(OpenEnumAPI.ProblemDetails.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersUserProfile.Failure.self)
            throw OpenEnumAPIClient.UsersUserProfile.Failure.notFound(payload)
        default:
            throw OpenEnumAPIClient.UsersUserProfile.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// Create a user
    public func createUser(_ body: OpenEnumAPIClient.UsersCreateUser.Body) async throws(OpenEnumAPIClient.UsersCreateUser.Failure) -> OpenEnumAPIClient.UsersCreateUser.Result {
        let path = "/users"
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.UsersCreateUser.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "POST"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        request.setValue("application/json", forHTTPHeaderField: "Content-Type")
        try Kizuna.encodeBody(&request, value: body.payload, using: client.encoder, failure: OpenEnumAPIClient.UsersCreateUser.Failure.self)
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.UsersCreateUser.Failure.self)
        switch statusCode {
        case 201:
            let body = try Kizuna.decode(OpenEnumAPI.User.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersCreateUser.Failure.self)
            return OpenEnumAPIClient.UsersCreateUser.Result(body: body)
        case 400:
            throw Kizuna.firstError(statusCode: statusCode, data: data, [
                { (try? client.decoder.decode(OpenEnumAPI.ProblemDetails.self, from: data)).map(OpenEnumAPIClient.UsersCreateUser.Failure.badRequest) },
                { (try? client.decoder.decode(OpenEnumAPIClient.ValidationError.self, from: data)).map(OpenEnumAPIClient.UsersCreateUser.Failure.validationError) },
            ])
        default:
            throw OpenEnumAPIClient.UsersCreateUser.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// Delete a user
    @available(*, deprecated, message: "use `archiveUser` instead")
    public func deleteUser(_ params: OpenEnumAPIClient.UsersDeleteUser.Params) async throws(OpenEnumAPIClient.UsersDeleteUser.Failure) -> OpenEnumAPIClient.UsersDeleteUser.Result {
        var path = "/users/:id"
        path = path.replacingOccurrences(of: ":id", with: Kizuna.encodePathSegment(params.id))
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.UsersDeleteUser.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "DELETE"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.UsersDeleteUser.Failure.self)
        switch statusCode {
        case 200:
            let body = try Kizuna.decode(OpenEnumAPIClient.UsersDeleteUser.Response.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersDeleteUser.Failure.self)
            return OpenEnumAPIClient.UsersDeleteUser.Result(body: body)
        case 404:
            let payload = try Kizuna.decode(OpenEnumAPI.ProblemDetails.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersDeleteUser.Failure.self)
            throw OpenEnumAPIClient.UsersDeleteUser.Failure.notFound(payload)
        default:
            throw OpenEnumAPIClient.UsersDeleteUser.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// Archive a user, first call returns 201, subsequent calls 200
    public func archiveUser(_ params: OpenEnumAPIClient.UsersArchiveUser.Params) async throws(OpenEnumAPIClient.UsersArchiveUser.Failure) -> OpenEnumAPIClient.UsersArchiveUser.Result {
        var path = "/users/:id/archive"
        path = path.replacingOccurrences(of: ":id", with: Kizuna.encodePathSegment(params.id))
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.UsersArchiveUser.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "POST"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.UsersArchiveUser.Failure.self)
        switch statusCode {
        case 200:
            let payload = try Kizuna.decode(OpenEnumAPIClient.UsersArchiveUser.Response.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersArchiveUser.Failure.self)
            return OpenEnumAPIClient.UsersArchiveUser.Result(body: .status200(payload))
        case 201:
            let payload = try Kizuna.decode(OpenEnumAPIClient.UsersArchiveUser.Response201.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersArchiveUser.Failure.self)
            return OpenEnumAPIClient.UsersArchiveUser.Result(body: .status201(payload))
        default:
            throw OpenEnumAPIClient.UsersArchiveUser.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// Upload an avatar image
    public func uploadAvatar(_ body: OpenEnumAPIClient.UsersUploadAvatar.Body) async throws(OpenEnumAPIClient.UsersUploadAvatar.Failure) -> OpenEnumAPIClient.UsersUploadAvatar.Result {
        let path = "/avatar"
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.UsersUploadAvatar.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "POST"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        var multipart = Kizuna.MultipartBuilder()
        multipart.appendFile(name: "file", file: body.file)
        multipart.appendField(name: "userId", value: String(describing: body.userId))
        request.httpBody = multipart.finalize()
        request.setValue(multipart.contentType, forHTTPHeaderField: "Content-Type")
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.UsersUploadAvatar.Failure.self)
        switch statusCode {
        case 200:
            let body = try Kizuna.decode(OpenEnumAPIClient.UsersUploadAvatar.Response.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersUploadAvatar.Failure.self)
            return OpenEnumAPIClient.UsersUploadAvatar.Result(body: body)
        case 400:
            let payload = try Kizuna.decode(OpenEnumAPIClient.ValidationError.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersUploadAvatar.Failure.self)
            throw OpenEnumAPIClient.UsersUploadAvatar.Failure.badRequest(payload)
        default:
            throw OpenEnumAPIClient.UsersUploadAvatar.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// Ping a user, exercises z.void() body and response
    public func pingUser(_ params: OpenEnumAPIClient.UsersPingUser.Params) async throws(OpenEnumAPIClient.UsersPingUser.Failure) {
        var path = "/users/:id/ping"
        path = path.replacingOccurrences(of: ":id", with: Kizuna.encodePathSegment(params.id))
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.UsersPingUser.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "POST"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.UsersPingUser.Failure.self)
        switch statusCode {
        case 204:
            return
        case 400:
            let payload = try Kizuna.decode(OpenEnumAPIClient.ValidationError.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersPingUser.Failure.self)
            throw OpenEnumAPIClient.UsersPingUser.Failure.badRequest(payload)
        default:
            throw OpenEnumAPIClient.UsersPingUser.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// List work items, exercises a z.void() arm in a multi-status success union and enum values that are not valid Swift identifiers
    public func getMyWork() async throws(OpenEnumAPIClient.UsersGetMyWork.Failure) -> OpenEnumAPIClient.UsersGetMyWork.Result {
        let path = "/work"
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.UsersGetMyWork.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "GET"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.UsersGetMyWork.Failure.self)
        switch statusCode {
        case 200:
            let payload = try Kizuna.decode(OpenEnumAPIClient.UsersGetMyWork.Response.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersGetMyWork.Failure.self)
            return OpenEnumAPIClient.UsersGetMyWork.Result(body: .status200(payload))
        case 204:
            return OpenEnumAPIClient.UsersGetMyWork.Result(body: .status204)
        default:
            throw OpenEnumAPIClient.UsersGetMyWork.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// Check user existence, exercises HEAD body stripping
    public func checkUser(_ params: OpenEnumAPIClient.UsersCheckUser.Params) async throws(OpenEnumAPIClient.UsersCheckUser.Failure) {
        var path = "/users/:id/check"
        path = path.replacingOccurrences(of: ":id", with: Kizuna.encodePathSegment(params.id))
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.UsersCheckUser.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "HEAD"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.UsersCheckUser.Failure.self)
        switch statusCode {
        case 200:
            return
        case 404:
            let payload = try Kizuna.decode(OpenEnumAPI.ProblemDetails.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersCheckUser.Failure.self)
            throw OpenEnumAPIClient.UsersCheckUser.Failure.notFound(payload)
        default:
            throw OpenEnumAPIClient.UsersCheckUser.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// Describe allowed operations, exercises OPTIONS routing
    public func describeUsers() async throws(OpenEnumAPIClient.UsersDescribeUsers.Failure) -> OpenEnumAPIClient.UsersDescribeUsers.Result {
        let path = "/users/describe"
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.UsersDescribeUsers.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "OPTIONS"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.UsersDescribeUsers.Failure.self)
        switch statusCode {
        case 200:
            let body = try Kizuna.decode(OpenEnumAPIClient.UsersDescribeUsers.Response.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.UsersDescribeUsers.Failure.self)
            return OpenEnumAPIClient.UsersDescribeUsers.Result(body: body)
        default:
            throw OpenEnumAPIClient.UsersDescribeUsers.Failure.unexpectedStatus(statusCode, data)
        }
    }
}

public struct OpenEnumAPIHealthClient: Sendable {
    private let client: OpenEnumAPIClient

    init(client: OpenEnumAPIClient) {
        self.client = client
    }

    /// Health check, exercises nested sub-client routing
    public func check() async throws(OpenEnumAPIClient.HealthCheck.Failure) -> OpenEnumAPIClient.HealthCheck.Result {
        let path = "/health"
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.HealthCheck.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "GET"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.HealthCheck.Failure.self)
        switch statusCode {
        case 200:
            let body = try Kizuna.decode(OpenEnumAPIClient.HealthCheck.Response.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.HealthCheck.Failure.self)
            return OpenEnumAPIClient.HealthCheck.Result(body: body)
        default:
            throw OpenEnumAPIClient.HealthCheck.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// Version, exercises second method in a sub-client group
    public func version() async throws(OpenEnumAPIClient.HealthVersion.Failure) -> OpenEnumAPIClient.HealthVersion.Result {
        let path = "/health/version"
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.HealthVersion.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "GET"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.HealthVersion.Failure.self)
        switch statusCode {
        case 200:
            let body = try Kizuna.decode(OpenEnumAPIClient.HealthVersion.Response.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.HealthVersion.Failure.self)
            return OpenEnumAPIClient.HealthVersion.Result(body: body)
        default:
            throw OpenEnumAPIClient.HealthVersion.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// Health history, exercises array return type qualification
    public func history() async throws(OpenEnumAPIClient.HealthHistory.Failure) -> OpenEnumAPIClient.HealthHistory.Result {
        let path = "/health/history"
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.HealthHistory.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "GET"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.HealthHistory.Failure.self)
        switch statusCode {
        case 200:
            let body = try Kizuna.decode([OpenEnumAPIClient.HealthHistory.ResponseItem].self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.HealthHistory.Failure.self)
            return OpenEnumAPIClient.HealthHistory.Result(body: body)
        default:
            throw OpenEnumAPIClient.HealthHistory.Failure.unexpectedStatus(statusCode, data)
        }
    }
}

public struct OpenEnumAPINotificationsClient: Sendable {
    private let client: OpenEnumAPIClient

    init(client: OpenEnumAPIClient) {
        self.client = client
    }

    /// Send a notification (discriminated by channel)
    public func sendNotification(_ body: OpenEnumAPIClient.NotificationsSendNotification.Body) async throws(OpenEnumAPIClient.NotificationsSendNotification.Failure) -> OpenEnumAPIClient.NotificationsSendNotification.Result {
        let path = "/notifications"
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.NotificationsSendNotification.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "POST"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        request.setValue("application/json", forHTTPHeaderField: "Content-Type")
        try Kizuna.encodeBody(&request, value: body.payload, using: client.encoder, failure: OpenEnumAPIClient.NotificationsSendNotification.Failure.self)
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.NotificationsSendNotification.Failure.self)
        switch statusCode {
        case 202:
            let body = try Kizuna.decode(OpenEnumAPIClient.NotificationsSendNotification.Response202.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.NotificationsSendNotification.Failure.self)
            return OpenEnumAPIClient.NotificationsSendNotification.Result(body: body)
        case 400:
            let payload = try Kizuna.decode(OpenEnumAPIClient.ValidationError.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.NotificationsSendNotification.Failure.self)
            throw OpenEnumAPIClient.NotificationsSendNotification.Failure.badRequest(payload)
        default:
            throw OpenEnumAPIClient.NotificationsSendNotification.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// List events, exercises Date / enum / array query params
    public func listEvents(_ query: OpenEnumAPIClient.NotificationsListEvents.Query = .query()) async throws(OpenEnumAPIClient.NotificationsListEvents.Failure) -> OpenEnumAPIClient.NotificationsListEvents.Result {
        let path = "/events"
        var queryItems: [URLQueryItem] = []
        queryItems += Kizuna.queryItems(name: "since", value: query.since)
        queryItems += Kizuna.queryItems(name: "kind", value: query.kind)
        queryItems += Kizuna.queryItems(name: "ids", value: query.ids)
        queryItems += Kizuna.queryItems(name: "label", value: query.label)
        queryItems += Kizuna.queryItems(name: "tagIds", value: query.tagIds)
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: queryItems, failure: OpenEnumAPIClient.NotificationsListEvents.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "GET"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.NotificationsListEvents.Failure.self)
        switch statusCode {
        case 200:
            let body = try Kizuna.decode(OpenEnumAPIClient.NotificationsListEvents.Response.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.NotificationsListEvents.Failure.self)
            return OpenEnumAPIClient.NotificationsListEvents.Result(body: body)
        case 400:
            let payload = try Kizuna.decode(OpenEnumAPIClient.ValidationError.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.NotificationsListEvents.Failure.self)
            throw OpenEnumAPIClient.NotificationsListEvents.Failure.badRequest(payload)
        default:
            throw OpenEnumAPIClient.NotificationsListEvents.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// Validate contract, exercises generator bug coverage
    public func validateConfig(_ body: OpenEnumAPIClient.NotificationsValidateConfig.Body) async throws(OpenEnumAPIClient.NotificationsValidateConfig.Failure) -> OpenEnumAPIClient.NotificationsValidateConfig.Result {
        let path = "/contract/validate"
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.NotificationsValidateConfig.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "POST"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        request.setValue("application/json", forHTTPHeaderField: "Content-Type")
        try Kizuna.encodeBody(&request, value: body.payload, using: client.encoder, failure: OpenEnumAPIClient.NotificationsValidateConfig.Failure.self)
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.NotificationsValidateConfig.Failure.self)
        switch statusCode {
        case 200:
            let body = try Kizuna.decode(OpenEnumAPIClient.NotificationsValidateConfig.Response.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.NotificationsValidateConfig.Failure.self)
            return OpenEnumAPIClient.NotificationsValidateConfig.Result(body: body)
        case 400:
            throw Kizuna.firstError(statusCode: statusCode, data: data, [
                { (try? client.decoder.decode(OpenEnumAPI.ProblemDetails.self, from: data)).map(OpenEnumAPIClient.NotificationsValidateConfig.Failure.badRequest) },
                { (try? client.decoder.decode(OpenEnumAPIClient.ValidationError.self, from: data)).map(OpenEnumAPIClient.NotificationsValidateConfig.Failure.validationError) },
            ])
        case 401:
            throw OpenEnumAPIClient.NotificationsValidateConfig.Failure.unauthorized
        default:
            throw OpenEnumAPIClient.NotificationsValidateConfig.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// Receive arbitrary webhook payload, exercises z.any() / AnyCodable codegen
    public func webhook(_ body: OpenEnumAPIClient.NotificationsWebhook.Body) async throws(OpenEnumAPIClient.NotificationsWebhook.Failure) -> OpenEnumAPIClient.NotificationsWebhook.Result {
        let path = "/webhook"
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.NotificationsWebhook.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "POST"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        request.setValue("application/json", forHTTPHeaderField: "Content-Type")
        try Kizuna.encodeBody(&request, value: body.payload, using: client.encoder, failure: OpenEnumAPIClient.NotificationsWebhook.Failure.self)
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.NotificationsWebhook.Failure.self)
        switch statusCode {
        case 200:
            let body = try Kizuna.decode(OpenEnumAPIClient.NotificationsWebhook.Response.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.NotificationsWebhook.Failure.self)
            return OpenEnumAPIClient.NotificationsWebhook.Result(body: body)
        case 400:
            let payload = try Kizuna.decode(OpenEnumAPIClient.ValidationError.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.NotificationsWebhook.Failure.self)
            throw OpenEnumAPIClient.NotificationsWebhook.Failure.badRequest(payload)
        default:
            throw OpenEnumAPIClient.NotificationsWebhook.Failure.unexpectedStatus(statusCode, data)
        }
    }
}

public struct OpenEnumAPIMembersClient: Sendable {
    private let client: OpenEnumAPIClient

    init(client: OpenEnumAPIClient) {
        self.client = client
    }

    /// List workspace members
    public func listMembers() async throws(OpenEnumAPIClient.MembersListMembers.Failure) -> OpenEnumAPIClient.MembersListMembers.Result {
        let path = "/workspace/members"
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.MembersListMembers.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "GET"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.MembersListMembers.Failure.self)
        switch statusCode {
        case 200:
            let body = try Kizuna.decode(OpenEnumAPIClient.MembersListMembers.Response.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.MembersListMembers.Failure.self)
            return OpenEnumAPIClient.MembersListMembers.Result(body: body)
        default:
            throw OpenEnumAPIClient.MembersListMembers.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// Invite a member to the workspace
    public func inviteMember(_ body: OpenEnumAPIClient.MembersInviteMember.Body) async throws(OpenEnumAPIClient.MembersInviteMember.Failure) -> OpenEnumAPIClient.MembersInviteMember.Result {
        let path = "/workspace/members"
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.MembersInviteMember.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "POST"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        request.setValue("application/json", forHTTPHeaderField: "Content-Type")
        try Kizuna.encodeBody(&request, value: body.payload, using: client.encoder, failure: OpenEnumAPIClient.MembersInviteMember.Failure.self)
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.MembersInviteMember.Failure.self)
        switch statusCode {
        case 201:
            let body = try Kizuna.decode(OpenEnumAPI.User.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.MembersInviteMember.Failure.self)
            return OpenEnumAPIClient.MembersInviteMember.Result(body: body)
        case 409:
            let payload = try Kizuna.decode(OpenEnumAPI.ProblemDetails.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.MembersInviteMember.Failure.self)
            throw OpenEnumAPIClient.MembersInviteMember.Failure.conflict(payload)
        case 400:
            let payload = try Kizuna.decode(OpenEnumAPIClient.ValidationError.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.MembersInviteMember.Failure.self)
            throw OpenEnumAPIClient.MembersInviteMember.Failure.badRequest(payload)
        default:
            throw OpenEnumAPIClient.MembersInviteMember.Failure.unexpectedStatus(statusCode, data)
        }
    }
}

public struct OpenEnumAPIWorkspaceClient: Sendable {
    private let client: OpenEnumAPIClient

    init(client: OpenEnumAPIClient) {
        self.client = client
    }

    /// Get workspace info
    public func getWorkspace() async throws(OpenEnumAPIClient.WorkspaceGetWorkspace.Failure) -> OpenEnumAPIClient.WorkspaceGetWorkspace.Result {
        let path = "/workspace"
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.WorkspaceGetWorkspace.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "GET"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.WorkspaceGetWorkspace.Failure.self)
        switch statusCode {
        case 200:
            let body = try Kizuna.decode(OpenEnumAPIClient.WorkspaceGetWorkspace.Response.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.WorkspaceGetWorkspace.Failure.self)
            return OpenEnumAPIClient.WorkspaceGetWorkspace.Result(body: body)
        default:
            throw OpenEnumAPIClient.WorkspaceGetWorkspace.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// Delete the workspace, owner-only via the auth map
    public func deleteWorkspace() async throws(OpenEnumAPIClient.WorkspaceDeleteWorkspace.Failure) -> OpenEnumAPIClient.WorkspaceDeleteWorkspace.Result {
        let path = "/workspace"
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.WorkspaceDeleteWorkspace.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "DELETE"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.WorkspaceDeleteWorkspace.Failure.self)
        switch statusCode {
        case 200:
            let body = try Kizuna.decode(OpenEnumAPIClient.WorkspaceDeleteWorkspace.Response.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.WorkspaceDeleteWorkspace.Failure.self)
            return OpenEnumAPIClient.WorkspaceDeleteWorkspace.Result(body: body)
        default:
            throw OpenEnumAPIClient.WorkspaceDeleteWorkspace.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// Transfer ownership, owner-only via the auth map
    public func transfer(_ body: OpenEnumAPIClient.WorkspaceTransfer.Body) async throws(OpenEnumAPIClient.WorkspaceTransfer.Failure) -> OpenEnumAPIClient.WorkspaceTransfer.Result {
        let path = "/workspace/transfer"
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.WorkspaceTransfer.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "POST"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        request.setValue("application/json", forHTTPHeaderField: "Content-Type")
        try Kizuna.encodeBody(&request, value: body.payload, using: client.encoder, failure: OpenEnumAPIClient.WorkspaceTransfer.Failure.self)
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.WorkspaceTransfer.Failure.self)
        switch statusCode {
        case 200:
            let body = try Kizuna.decode(OpenEnumAPIClient.WorkspaceTransfer.Response.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.WorkspaceTransfer.Failure.self)
            return OpenEnumAPIClient.WorkspaceTransfer.Result(body: body)
        case 400:
            let payload = try Kizuna.decode(OpenEnumAPIClient.ValidationError.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.WorkspaceTransfer.Failure.self)
            throw OpenEnumAPIClient.WorkspaceTransfer.Failure.badRequest(payload)
        default:
            throw OpenEnumAPIClient.WorkspaceTransfer.Failure.unexpectedStatus(statusCode, data)
        }
    }
}

public struct OpenEnumAPIInvitesClient: Sendable {
    private let client: OpenEnumAPIClient

    init(client: OpenEnumAPIClient) {
        self.client = client
    }

    /// Resolve an invite by its capability-URL token, guarded by a custom path-token identity
    public func getInvite(_ params: OpenEnumAPIClient.InvitesGetInvite.Params) async throws(OpenEnumAPIClient.InvitesGetInvite.Failure) -> OpenEnumAPIClient.InvitesGetInvite.Result {
        var path = "/invites/:token"
        path = path.replacingOccurrences(of: ":token", with: Kizuna.encodePathSegment(params.token))
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.InvitesGetInvite.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "GET"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.InvitesGetInvite.Failure.self)
        switch statusCode {
        case 200:
            let body = try Kizuna.decode(OpenEnumAPIClient.InvitesGetInvite.Response.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.InvitesGetInvite.Failure.self)
            return OpenEnumAPIClient.InvitesGetInvite.Result(body: body)
        case 404:
            let payload = try Kizuna.decode(OpenEnumAPI.ProblemDetails.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.InvitesGetInvite.Failure.self)
            throw OpenEnumAPIClient.InvitesGetInvite.Failure.notFound(payload)
        default:
            throw OpenEnumAPIClient.InvitesGetInvite.Failure.unexpectedStatus(statusCode, data)
        }
    }

    /// Accept an invite via the capability URL
    public func acceptInvite(_ params: OpenEnumAPIClient.InvitesAcceptInvite.Params, _ body: OpenEnumAPIClient.InvitesAcceptInvite.Body) async throws(OpenEnumAPIClient.InvitesAcceptInvite.Failure) -> OpenEnumAPIClient.InvitesAcceptInvite.Result {
        var path = "/invites/:token/accept"
        path = path.replacingOccurrences(of: ":token", with: Kizuna.encodePathSegment(params.token))
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.InvitesAcceptInvite.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "POST"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        request.setValue("application/json", forHTTPHeaderField: "Content-Type")
        try Kizuna.encodeBody(&request, value: body.payload, using: client.encoder, failure: OpenEnumAPIClient.InvitesAcceptInvite.Failure.self)
        let (data, statusCode, _) = try await Kizuna.send(&request, session: client.session, requestMiddleware: client.requestMiddleware, responseMiddleware: client.responseMiddleware, failure: OpenEnumAPIClient.InvitesAcceptInvite.Failure.self)
        switch statusCode {
        case 201:
            let body = try Kizuna.decode(OpenEnumAPIClient.InvitesAcceptInvite.Response201.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.InvitesAcceptInvite.Failure.self)
            return OpenEnumAPIClient.InvitesAcceptInvite.Result(body: body)
        case 404:
            let payload = try Kizuna.decode(OpenEnumAPI.ProblemDetails.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.InvitesAcceptInvite.Failure.self)
            throw OpenEnumAPIClient.InvitesAcceptInvite.Failure.notFound(payload)
        case 400:
            let payload = try Kizuna.decode(OpenEnumAPIClient.ValidationError.self, from: data, using: client.decoder, statusCode: statusCode, failure: OpenEnumAPIClient.InvitesAcceptInvite.Failure.self)
            throw OpenEnumAPIClient.InvitesAcceptInvite.Failure.badRequest(payload)
        default:
            throw OpenEnumAPIClient.InvitesAcceptInvite.Failure.unexpectedStatus(statusCode, data)
        }
    }
}

public struct OpenEnumAPIAssistantClient: Sendable {
    private let client: OpenEnumAPIClient

    init(client: OpenEnumAPIClient) {
        self.client = client
    }

    /// Stream an assistant reply, exercises a server-sent events response
    public func reply(_ body: OpenEnumAPIClient.AssistantReply.Body) async throws(OpenEnumAPIClient.AssistantReply.Failure) -> OpenEnumAPIClient.AssistantReply.Result {
        let path = "/assistant/reply"
        let url = try Kizuna.makeURL(baseURL: client.baseURL, path: path, queryItems: [], failure: OpenEnumAPIClient.AssistantReply.Failure.self)
        var request = URLRequest(url: url, cachePolicy: .useProtocolCachePolicy, timeoutInterval: client.timeout)
        request.httpMethod = "POST"
        for (name, value) in client.requestContextHeaders { request.setValue(value, forHTTPHeaderField: name) }
        request.setValue("application/json", forHTTPHeaderField: "Content-Type")
        try Kizuna.encodeBody(&request, value: body.payload, using: client.encoder, failure: OpenEnumAPIClient.AssistantReply.Failure.self)
        let (bytes, statusCode, _) = try await Kizuna.open(&request, session: client.session, requestMiddleware: client.requestMiddleware, failure: OpenEnumAPIClient.AssistantReply.Failure.self)
        switch statusCode {
        case 200:
            let body = Kizuna.events(bytes, using: client.decoder) { event, decoder -> OpenEnumAPIClient.AssistantReply.Event? in
                switch event.event {
                case "delta": return .delta(try decoder.decode(OpenEnumAPIClient.AssistantReply.Delta.self, from: Foundation.Data(event.data.utf8)))
                case "done": return .done(try decoder.decode(OpenEnumAPIClient.AssistantReply.Done.self, from: Foundation.Data(event.data.utf8)))
                case "tool_call": return .tool_call(try decoder.decode(OpenEnumAPIClient.AssistantReply.ToolCall.self, from: Foundation.Data(event.data.utf8)))
                case "tool_result": return .tool_result(try decoder.decode(OpenEnumAPIClient.AssistantReply.ToolResult.self, from: Foundation.Data(event.data.utf8)))
                case "tool_error": return .tool_error(try decoder.decode(OpenEnumAPIClient.AssistantReply.ToolError.self, from: Foundation.Data(event.data.utf8)))
                default: return nil
                }
            }
            return OpenEnumAPIClient.AssistantReply.Result(body: body)
        case 400:
            let data = try await Kizuna.collect(bytes, failure: OpenEnumAPIClient.AssistantReply.Failure.self)
            throw Kizuna.firstError(statusCode: statusCode, data: data, [
                { (try? client.decoder.decode(OpenEnumAPI.ProblemDetails.self, from: data)).map(OpenEnumAPIClient.AssistantReply.Failure.badRequest) },
                { (try? client.decoder.decode(OpenEnumAPIClient.ValidationError.self, from: data)).map(OpenEnumAPIClient.AssistantReply.Failure.validationError) },
            ])
        default:
            let data = try await Kizuna.collect(bytes, failure: OpenEnumAPIClient.AssistantReply.Failure.self)
            throw OpenEnumAPIClient.AssistantReply.Failure.unexpectedStatus(statusCode, data)
        }
    }
}

public protocol KizunaFailure: Swift.Error {
    static func requestFailed(_ error: Swift.Error) -> Self
    static var invalidRequest: Self { get }
    static var cancelled: Self { get }
    static var invalidResponse: Self { get }
    static func unexpectedStatus(_ status: Int, _ data: Foundation.Data) -> Self
    var isCancelled: Bool { get }
}

public protocol KizunaDecodableFailure: KizunaFailure {
    static func decoding(_ error: Swift.Error, statusCode: Int, data: Foundation.Data) -> Self
}

private enum Kizuna {
    @Sendable static func decodeDate(_ decoder: Decoder) throws -> Date {
        let container = try decoder.singleValueContainer()
        let raw = try container.decode(String.self)
        if let date = try? Date(raw, strategy: Date.ISO8601FormatStyle(includingFractionalSeconds: true)) { return date }
        if let date = try? Date(raw, strategy: Date.ISO8601FormatStyle()) { return date }
        throw DecodingError.dataCorruptedError(in: container, debugDescription: "Invalid ISO8601 date: \(raw)")
    }

    static func encodeDate(_ date: Date) -> String {
        date.formatted(Date.ISO8601FormatStyle(includingFractionalSeconds: true))
    }

    static func makeJSONEncoder() -> JSONEncoder {
        let encoder = JSONEncoder()
        encoder.dateEncodingStrategy = .custom { date, encoder in
            var container = encoder.singleValueContainer()
            try container.encode(encodeDate(date))
        }
        return encoder
    }

    static func makeJSONDecoder() -> JSONDecoder {
        let decoder = JSONDecoder()
        decoder.dateDecodingStrategy = .custom(decodeDate)
        return decoder
    }

    private static let pathSegmentAllowed: CharacterSet = {
        var allowed = CharacterSet.urlPathAllowed
        allowed.remove(charactersIn: "/")
        return allowed
    }()

    static func encodePathSegment<Value>(_ value: Value) -> String {
        let text = stringifyQueryValue(value).first ?? ""
        return text.addingPercentEncoding(withAllowedCharacters: pathSegmentAllowed) ?? text
    }

    static func appendPath(_ base: URL, _ path: String) -> URL {
        base.appending(path: path, directoryHint: .notDirectory)
    }

    static func stringifyQueryValue(_ value: Any) -> [String] {
        switch value {
        case let date as Date:
            return [encodeDate(date)]
        case let array as [Any]:
            return array.flatMap { stringifyQueryValue($0) }
        case let raw as any RawRepresentable where raw.rawValue is CustomStringConvertible:
            return [String(describing: raw.rawValue)]
        case let convertible as CustomStringConvertible:
            return [convertible.description]
        default:
            return [String(describing: value)]
        }
    }

    static func queryItems<Value>(name: String, value: Value?) -> [URLQueryItem] {
        guard let value else { return [] }
        return stringifyQueryValue(value).map { URLQueryItem(name: name, value: $0) }
    }

    static func setHeader<Value>(_ request: inout URLRequest, name: String, value: Value?) {
        guard let value else { return }
        request.setValue(stringifyQueryValue(value).joined(separator: ", "), forHTTPHeaderField: name)
    }

    static func makeURL<Failure: KizunaFailure>(baseURL: URL, path: String, queryItems: [URLQueryItem], failure: Failure.Type) throws(Failure) -> URL {
        guard var components = URLComponents(url: appendPath(baseURL, path), resolvingAgainstBaseURL: false) else {
            throw Failure.invalidRequest
        }
        if !queryItems.isEmpty { components.queryItems = queryItems }
        guard let url = components.url else { throw Failure.invalidRequest }
        return url
    }

    static func encodeBody<Value: Encodable, Failure: KizunaFailure>(_ request: inout URLRequest, value: Value, using encoder: JSONEncoder, failure: Failure.Type) throws(Failure) {
        do { request.httpBody = try encoder.encode(value) }
        catch { throw Failure.requestFailed(error) }
    }

    static func send<Failure: KizunaFailure>(_ request: inout URLRequest, session: URLSession, requestMiddleware: (@Sendable (inout URLRequest) async throws -> Void)?, responseMiddleware: (@Sendable (URLRequest, Foundation.Data, URLResponse) async -> Void)?, failure: Failure.Type) async throws(Failure) -> (Foundation.Data, Int, HTTPURLResponse) {
        if let requestMiddleware {
            do { try await requestMiddleware(&request) }
            catch is CancellationError { throw Failure.cancelled }
            catch let error as URLError where error.code == .cancelled { throw Failure.cancelled }
            catch { throw Failure.requestFailed(error) }
        }
        let data: Foundation.Data
        let response: URLResponse
        do { (data, response) = try await session.data(for: request) }
        catch is CancellationError { throw Failure.cancelled }
        catch let error as URLError where error.code == .cancelled { throw Failure.cancelled }
        catch { throw Failure.requestFailed(error) }
        if let responseMiddleware { await responseMiddleware(request, data, response) }
        guard let httpResponse = response as? HTTPURLResponse else { throw Failure.invalidResponse }
        return (data, httpResponse.statusCode, httpResponse)
    }

    static func decode<Value: Decodable, Failure: KizunaDecodableFailure>(_ type: Value.Type, from data: Foundation.Data, using decoder: JSONDecoder, statusCode: Int, failure: Failure.Type) throws(Failure) -> Value {
        do { return try decoder.decode(Value.self, from: data) }
        catch { throw Failure.decoding(error, statusCode: statusCode, data: data) }
    }

    static func firstError<Failure: KizunaDecodableFailure>(statusCode: Int, data: Foundation.Data, _ attempts: [() -> Failure?]) -> Failure {
        for attempt in attempts {
            if let failure = attempt() { return failure }
        }
        return Failure.decoding(DecodingError.dataCorrupted(.init(codingPath: [], debugDescription: "No matching type for status \(statusCode)")), statusCode: statusCode, data: data)
    }

    struct ServerSentEvent: Sendable {
        let event: String?
        let data: String
        let id: String?
        let retry: Int?
    }

    static func open<Failure: KizunaFailure>(_ request: inout URLRequest, session: URLSession, requestMiddleware: (@Sendable (inout URLRequest) async throws -> Void)?, failure: Failure.Type) async throws(Failure) -> (URLSession.AsyncBytes, Int, HTTPURLResponse) {
        if let requestMiddleware {
            do { try await requestMiddleware(&request) }
            catch is CancellationError { throw Failure.cancelled }
            catch let error as URLError where error.code == .cancelled { throw Failure.cancelled }
            catch { throw Failure.requestFailed(error) }
        }
        let bytes: URLSession.AsyncBytes
        let response: URLResponse
        do { (bytes, response) = try await session.bytes(for: request) }
        catch is CancellationError { throw Failure.cancelled }
        catch let error as URLError where error.code == .cancelled { throw Failure.cancelled }
        catch { throw Failure.requestFailed(error) }
        guard let httpResponse = response as? HTTPURLResponse else { throw Failure.invalidResponse }
        return (bytes, httpResponse.statusCode, httpResponse)
    }

    static func collect<Failure: KizunaFailure>(_ bytes: URLSession.AsyncBytes, failure: Failure.Type) async throws(Failure) -> Foundation.Data {
        var data = Foundation.Data()
        do { for try await byte in bytes { data.append(byte) } }
        catch is CancellationError { throw Failure.cancelled }
        catch { throw Failure.requestFailed(error) }
        return data
    }

    static func lines(_ bytes: URLSession.AsyncBytes) -> AsyncThrowingStream<String, Swift.Error> {
        AsyncThrowingStream { continuation in
            let task = Task {
                var buffer: [UInt8] = []
                var pendingCarriageReturn = false
                do {
                    for try await byte in bytes {
                        if pendingCarriageReturn {
                            pendingCarriageReturn = false
                            if byte == 0x0A { continue }
                        }
                        if byte == 0x0A || byte == 0x0D {
                            pendingCarriageReturn = byte == 0x0D
                            continuation.yield(String(decoding: buffer, as: UTF8.self))
                            buffer.removeAll(keepingCapacity: true)
                            continue
                        }
                        buffer.append(byte)
                    }
                    if !buffer.isEmpty { continuation.yield(String(decoding: buffer, as: UTF8.self)) }
                    continuation.finish()
                }
                catch { continuation.finish(throwing: error) }
            }
            continuation.onTermination = { _ in task.cancel() }
        }
    }

    static func chunks(_ bytes: URLSession.AsyncBytes) -> AsyncThrowingStream<Foundation.Data, Swift.Error> {
        AsyncThrowingStream { continuation in
            let task = Task {
                var buffer = Foundation.Data()
                do {
                    for try await byte in bytes {
                        buffer.append(byte)
                        if buffer.count >= 16_384 {
                            continuation.yield(buffer)
                            buffer = Foundation.Data()
                        }
                    }
                    if !buffer.isEmpty { continuation.yield(buffer) }
                    continuation.finish()
                }
                catch { continuation.finish(throwing: error) }
            }
            continuation.onTermination = { _ in task.cancel() }
        }
    }

    static func serverSentEvents(_ bytes: URLSession.AsyncBytes) -> AsyncThrowingStream<ServerSentEvent, Swift.Error> {
        AsyncThrowingStream { continuation in
            let task = Task {
                var eventType = ""
                var dataLines: [String] = []
                var lastEventId = ""
                var retry: Int? = nil
                do {
                    for try await line in lines(bytes) {
                        if line.isEmpty {
                            if !dataLines.isEmpty {
                                continuation.yield(ServerSentEvent(event: eventType.isEmpty ? nil : eventType, data: dataLines.joined(separator: "\n"), id: lastEventId.isEmpty ? nil : lastEventId, retry: retry))
                                dataLines = []
                                retry = nil
                            }
                            eventType = ""
                            continue
                        }
                        if line.hasPrefix(":") { continue }
                        let field: Substring
                        var value: Substring
                        if let separator = line.firstIndex(of: ":") {
                            field = line[..<separator]
                            value = line[line.index(after: separator)...]
                            if value.hasPrefix(" ") { value = value.dropFirst() }
                        } else {
                            field = Substring(line)
                            value = ""
                        }
                        switch field {
                        case "event": eventType = String(value)
                        case "data": dataLines.append(String(value))
                        case "id": if !value.contains("\0") { lastEventId = String(value) }
                        case "retry": if !value.isEmpty, value.allSatisfy(\.isNumber), let milliseconds = Int(value) { retry = milliseconds }
                        default: break
                        }
                    }
                    continuation.finish()
                }
                catch { continuation.finish(throwing: error) }
            }
            continuation.onTermination = { _ in task.cancel() }
        }
    }

    static func events<Event: Sendable>(_ bytes: URLSession.AsyncBytes, using decoder: JSONDecoder, _ decode: @escaping @Sendable (ServerSentEvent, JSONDecoder) throws -> Event?) -> AsyncThrowingStream<Event, Swift.Error> {
        AsyncThrowingStream { continuation in
            let task = Task {
                do {
                    for try await event in serverSentEvents(bytes) {
                        if let decoded = try decode(event, decoder) { continuation.yield(decoded) }
                    }
                    continuation.finish()
                }
                catch { continuation.finish(throwing: error) }
            }
            continuation.onTermination = { _ in task.cancel() }
        }
    }

    struct MultipartBuilder {
        let boundary: String
        private var data = Foundation.Data()

        init(boundary: String = "kizuna-\(UUID().uuidString)") {
            self.boundary = boundary
        }

        mutating func appendField(name: String, value: String) {
            data.append("--\(boundary)\r\n".data(using: .utf8)!)
            data.append("Content-Disposition: form-data; name=\"\(name)\"\r\n\r\n".data(using: .utf8)!)
            data.append(value.data(using: .utf8)!)
            data.append("\r\n".data(using: .utf8)!)
        }

        mutating func appendFile(name: String, file: OpenEnumAPIClient.MultipartFile) {
            data.append("--\(boundary)\r\n".data(using: .utf8)!)
            data.append("Content-Disposition: form-data; name=\"\(name)\"; filename=\"\(file.filename)\"\r\n".data(using: .utf8)!)
            data.append("Content-Type: \(file.mimeType)\r\n\r\n".data(using: .utf8)!)
            data.append(file.data)
            data.append("\r\n".data(using: .utf8)!)
        }

        func finalize() -> Foundation.Data {
            var result = data
            result.append("--\(boundary)--\r\n".data(using: .utf8)!)
            return result
        }

        var contentType: String {
            "multipart/form-data; boundary=\(boundary)"
        }
    }
}
